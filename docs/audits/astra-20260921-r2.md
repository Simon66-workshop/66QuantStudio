# Astra 审计 2026-09-21 · R2 修复复验

**结论：CHANGES_REQUIRED。29条修复声明中，22条独立复验通过，7条部分修复；028/031延期，另新增1条。不能整体写成29条修好或产品全量放行。**

- 冻结main：`a0a1213bad9df74aa561ea2a8e2814b987fc2137`；上一轮基线：`ac3b8915488da0664d78a4b19afb262636735bbe`。
- 答复入口：[`cursor-20260921.md`](./cursor-20260921.md)；原报告保留。
- 本轮分支：`audit/astra-20260921-r2`；报告文件同日使用`-r2`，避免覆盖旧报告。

## 打开界面先看什么

桌面卡片已能独立抬起，原裁切问题不再复现；390/768菜单能打开数据库、设置等十个入口，发送按钮也不再竖排。这些改善接受。仍然一眼可见的是390px首页：520px高的区域内卡片整体缩到约0.394，正文视觉字号约4.33px，聚焦也未恢复可读。新增032：关闭比赛后确认按钮仍可点，后端正确拒绝但页面没有错误说明。更重要的剩余反例是默认信任的无关5173来源能确认模拟计划，以及只有多仓时平空却减多仓。模型已收到历史，但仍未收到右侧报告数据，合法长输入进入模型前仍被截到4000字符。29条fixed不能整体接受；22条复验通过、7条部分修复、028/031延期，另有1条新发现。

## 范围与执行证据

只复验原001—031修复/延期及新增的相邻错误路径032。不修改产品代码，不继承Cursor的完成结论，不做真实交易或实际付费模型调用。同日-r2文件/分支用于保留原报告，符合docs/audits/README.md不得覆盖历史的约定。

| 环节 | 本轮实际结果 |
|---|---|
| 取源码 | 容器直接clone发生GitHub DNS解析失败；授权GitHub Actions实际clone、git pull --ff-only、detach冻结SHA、安装/测试/构建/启动；下载并校验artifact，使用git bundle在本地重建同版源码。没有要求用户传文件。 |
| 浏览器环境 | 容器本地Chromium访问3198被管理策略返回net::ERR_BLOCKED_BY_ADMINISTRATOR，未绕过；本轮真实点击在GitHub Actions Chrome完成，仍使用所要求的127.0.0.1:3198。无Browser插件，使用常规Playwright。这不是用户电脑或iPhone真机验收。 |
| 版本 | Node22.16.0 / npm10.9.2 / Ubuntu Linux；Chrome152.0.7977.82 / Playwright1.55，含CJK字体。 |
| npm install | 干净runner exit0；仍改写web lock，见031。 |
| npm test | runner和本地独立执行均22/22、0失败，不继承Cursor测试结论。 |
| npm run build / start | 两环境build exit0；npm start，3198 health ok:true；真实浏览器打开并点击。 |
| 浏览器交互 | B20组、C6组均执行完成，不代表产品全部PASS；B无pageerror，C故意复现032产生contest-offline。 |
| 测试边界 | 仅本地mock/合成数据/模拟账户，没有真实模型、官方Jev、行情或柜台交易验证。 |

## 31条修复声明对账

`verified-fixed`仅针对本轮列明的原问题与验收样例，不是整个功能永远无缺陷。`partial`承认已修子项，同时保留剩余反例。

| ID | Cursor | Astra R2 | 观察及证据 |
|---|---|---|---|
| 001 | fixed | **partial** | 桌面裁切和hover/focus修复；390px整体缩放0.3937198，正文约4.33px，保留原ID继续修窄屏可读性。 证据：B:001-home,002-mobile; C:001-narrow-readability-and-links |
| 002 | fixed | **verified-fixed** | 390/768菜单十入口；实点数据库/设置、关抽屉、Esc通过。390四首页入口可点，数据库/设置/比赛/技能scrollWidth均390。 证据：B:002-mobile; C:001-narrow-readability-and-links |
| 003 | fixed | **verified-fixed** | 七预设实际改变stage/shell参数；背景关闭后shell ambient display=none。hero局部ambient仍保留，不能声称全部光效关闭；原只有保存无渲染变化问题已修。 证据：B:003-presets |
| 004 | fixed | **verified-fixed** | 通过技能入口实点发送和报告；发送按钮48.015625px、nowrap，不再竖排。 证据：B:004-025-report |
| 005 | fixed | **partial** | 3201来源与无CSRF写入阻断；默认5173来源却可读token、写设置、confirm，模拟fills1→2。 证据：B:005-browser-origin-boundary; L:HTTP |
| 006 | fixed | **verified-fixed** | 原始Host:[返回400 bad-host，后续health200，进程存活；坏百分号路径也400。 证据：L:malformed-host; A:npm-test |
| 007 | fixed | **verified-fixed** | 五次独立实例各并发24次创建，成功24/磁盘24/重启24；坏JSON留备份和recovery提示。未做OS断电注入。 证据：L:007-concurrent-persist,007-corrupt-recovery |
| 008 | fixed | **partial** | 原quoted multiline、重复列、未闭合引号、HTML伪装修复；引号内部空格仍丢、非法中途引号仍接受、合法含标签文本误拒绝。残余降P2。 证据：L:008-csv,008-extra-csv; B:008-023-csv-ui |
| 009 | fixed | **verified-fixed** | 100/空/110→n2、min100、last110、回撤0、missing-values；没有把空当0。 证据：L:009-missing-values |
| 010 | fixed | **verified-fixed** | 8日每日4标的常数因子→8个ic:null、stats:null、zero-variance可见。 证据：L:010-zero-variance |
| 011 | fixed | **verified-fixed** | Jan3=110/Jan1=100/Jan2=90乱序→last110、回撤-10%、unsorted-dates。 证据：L:011-unsorted-dates |
| 012 | fixed | **verified-fixed** | 单日4标的有IC摘要时，HTML仍显示insufficient-cross-sections、undefined-ir。 证据：L:012-insufficient-cross-sections |
| 013 | fixed | **verified-fixed** | 点名astra-price.csv的100/120/90样本绑定正确；最新90、回撤-25%，未误路由IC。 证据：L:013-upload-binding |
| 014 | fixed | **partial** | 正向合约/方向/手数与撤单修复；-3手被改成+3，否定句仍被当肯定计划。 证据：L:014-parser; B:014-016-trade-ui; C:014-negative-lots-ui |
| 015 | fixed | **partial** | 同向累加、独立合约开空与保证金/风险同步修复；只有long2时平空1手却执行并减成long1。 证据：L:014-016-ledger-plans; C:015-close-short-on-long-ui |
| 016 | fixed | **verified-fixed** | 过期拒绝plan-expired，离线confirm400且fills不增；取消不成交。前端吞错另列032，不推翻后端门通过。 证据：L:014-016-ledger-plans; C:032-offline-confirm-error |
| 017 | fixed | **verified-fixed** | 30个100加当前130，breakout产生open-long pending；窗口排除当前bar。 证据：L:017-018-jev-templates; B:017-018-jev-ui |
| 018 | fixed | **verified-fixed** | 三模板实点开始/采样/停止；单次与无后台调度说明明确；pending去重，停止取消未确认Jev计划，没有自动fill。 证据：L:017-018-jev-templates; B:017-018-jev-ui |
| 019 | fixed | **verified-fixed** | 设置保存mock URL/model后离开再进仍在；空key框再保存不覆盖旧key，GET仅last4。 证据：B:019-settings-roundtrip |
| 020 | fixed | **verified-fixed** | mock收到/v1/chat/completions；503与约8秒超时回退本地，消息/轨迹记录model-http-503/model-timeout。非真实模型可用性认证。 证据：L:020-021-model-capture |
| 021 | fixed | **partial** | 第二轮已有ALPHA历史和技能name/summary；仍没有SKILL约束正文、本地报告数据/统计，Dataset:unbound。 证据：L:020-021-model-capture |
| 022 | fixed | **verified-fixed** | 延迟mock下3次Ctrl+Enter只有1条user/1个artifact；同key并发3次服务端同样只写1次。 证据：B:022-shortcut; L:022-idempotency |
| 023 | fixed | **verified-fixed** | 消息503可见错误、保留输入、可重试；非法CSV可见错误；不存在会话有404且无composer。只关闭原列明路径，不代表所有mutation都处理了错误。 证据：B:023-errors,008-023-csv-ui |
| 024 | fixed | **verified-fixed** | 同名本机技能ID不同；卸载后不能开技能会话，UI可重新安装；未知技能拒绝。 证据：B:024-skills-ui; L:024-skill-lifecycle |
| 025 | fixed | **verified-fixed** | 新建后立即回列表能找到新会话；刷新报告页恢复最后iframe。 证据：B:025-list-refresh,004-025-report |
| 026 | fixed | **verified-fixed** | 第六个技能和首个专家详情均可收藏，列表可打开对应资源并取消，不再仅限前四项。 证据：B:026-favorites-ui |
| 027 | fixed | **verified-fixed** | 专家/专家团列表和详情显示快照缺省说明，不再空白LEAD；不等于补齐完整上游内容。 证据：B:027-catalog-gaps |
| 028 | deferred | **deferred** | root critical2包、web high2包仍在，含多条公告，不是4个独立漏洞。未证明本应用RCE；延期不是安全豁免。 证据：A:npm-audit-root.json,npm-audit-web.json |
| 029 | fixed | **partial** | API8001字拒绝已修；合法4521字到模型只剩4000、尾部丢失。UI超限粘贴也被maxlength裁剪，提示却称不会静默截断。 证据：B:029-long-ui; L:029-long,029-current-model-truncation |
| 030 | fixed | **verified-fixed** | 缺失JS返回404；导航仍200 HTML，实际JS/CSS MIME、nosniff、CSP正确。 证据：L:HTTP assets-and-SPA |
| 031 | deferred | **deferred** | 完整web锁文件和dist忽略仍未补；端口占用已给友好提示，这个子项应承认已修，不能照抄旧问题。 证据：A:install-working-tree.txt; L:port-in-use |

## 五条闸门

| 闸门 | 独立结论 |
|---|---|
| 产品 | PARTIAL: 014/021/029与新032；不能按29条全部修复放行。 |
| 设计 | PARTIAL: 001窄屏可读性；桌面hover及003预设修复接受。 |
| 后端 | PARTIAL: 015方向账本仍错误；031残余延期。 |
| 安全 | PARTIAL: 005默认信任开发端口仍可越权确认；028依赖延期。 |
| 数据 | PARTIAL: 数值、IC、种子表对照通过；008仍有CSV无损性/语法残余。 |

以下只展开未关闭部分和新发现，不重复原31条完整清单。P0按任务书的安全边界/错误模拟账本标准；不代表真实资金成交。008原P0错行已修，残余降为P2。

## 阻塞（P0）

### ASTRA-20260921-005 默认启动仍信任无关开发端口并允许其确认模拟交易

- 闸门：安全；严重度：P0；状态：`partial`。
- 位置：`server/lib/http-guard.mjs:10-24,49-66; server/index.mjs mutation guard`。

复现：
1. 隔离目录npm start，不设置QS_ORIGINS，也不启动本项目Vite；连接模拟账户并创建pending计划。
2. 另起仅用于审计的网页http://127.0.0.1:5173，从页面fetch 3198/api/bootstrap获取csrfToken。
3. 带token从该网页PUT设置并POST /api/contest/plans/<id>/confirm，不点击工作台确认按钮。
4. 对照3201来源重复读取。

**期望：** 普通启动仅信任自身来源；开发/预览来源需按显式启动模式授权，不能仅占用常见端口就拿到读写/确认权限。

**实际：** 真实Chrome：3201被阻断；无关5173读取200、写入200、confirm200，fills1→2，workspaceName被修改。默认allowlist无条件加入127/localhost的5173、4173。原通配CORS与默认0.0.0.0问题已修。仅证明另一loopback网页及本地模拟账户，不是所有互联网网站可利用或真实成交证明。

**修改建议：** 默认只开本服务origin；dev/preview确实启动时显式配置准确来源，敏感确认依赖受控会话授权，不能把token发给未授权来源；保留Host/loopback/CSRF新防护。

**建议补测：** 默认下真实浏览器3201/5173/4173的GET、预检、PUT、confirm；拒绝后设置/fills不变；开发模式另有正向用例。

证据：B:005-browser-origin-boundary；L:HTTP no-CSRF and Origin3201 controls。

### ASTRA-20260921-015 模拟账本仍把平空记成减多仓

- 闸门：后端；严重度：P0；状态：`partial`。
- 位置：`server/lib/studio.mjs:374-395,425-459,1096-1121`。

复现：
1. 隔离空账户启用模拟赛，创建并确认开多rb2610 2手，确认只有long2、没有short。
2. 输入平空 rb2610 1手，查看pending并点击确认。
3. 核对reason、side、fills、position和margin；另核对持多时open-short是否被隐式净额抵消。

**期望：** 平空必须要求足量空仓，无空仓则拒绝，不能减多仓；仅支持净仓也须明确转换或拒绝不支持的动作。

**实际：** UI实测reason平空rb2610 1手，side被降为close；确认后long2→long1、margin24000→12000、fills1→2。没有空仓。引擎还复现反向开仓隐式抵消。同向累加、独立合约开空和风险指标联动已修。仅本地模拟账本，不是实际柜台成交。

**修改建议：** 动作保留open-long/open-short/close-long/close-short；按合约与方向校验足量仓位、全部不变量，原子记账。若用净额模型必须在计划明确转换或拒绝歧义。

**建议补测：** 无空仓平空、无多仓平多、超量平仓、加仓、反向开仓、部分/全平、多合约、fills可重放一致。

证据：C:015-close-short-on-long-ui,r2-close-short-long-reduced.png；L:014-016-ledger-plans。


## 严重（P1）

### ASTRA-20260921-014 负数量与否定句仍被解析成肯定开仓计划

- 闸门：产品；严重度：P1；状态：`partial`。
- 位置：`server/lib/studio.mjs:374-395`。

复现：
1. 模拟赛会话发开多 rb2610 -3手，只看计划，不确认。
2. 对parseTradeIntent再输入不要开多 rb2610 3手及小数手数。

**期望：** 完整解析有符号数量并校验支持的正整数；否定、冲突或不可靠意图不创建计划，应拒绝/澄清。

**实际：** UI的-3手生成open-long×3 pending，负号被跳过；引擎将不要开多解释为place/open-long，小数可成为lots。正向合约/方向/手数与撤单样例已修。确认门仍在，没有因本例自动成交。

**修改建议：** 先识别完整数字token和符号，校验整数范围；否定/询问不能当执行意图，不用子串和仅匹配正数regex兜底。

**建议补测：** 正整数/0/负数/小数/缺失数量/否定/反问，拒绝不产生pending，fills不变。

证据：C:014-negative-lots-ui,r2-negative-quantity-pending.png；L:014-parser。

### ASTRA-20260921-021 历史已补，模型仍未获得右侧报告的数据上下文

- 闸门：后端；严重度：P1；状态：`partial`。
- 位置：`server/lib/studio.mjs:846-889; maybeModel(conv,userText,local)`。

复现：
1. 使用隔离本地HTTP mock作为模型服务，绑定skill-factor-evaluate开会话。
2. 先发记住代号ALPHA，分析动量因子并生成右侧报告。
3. 再问代号与右侧报告IC数值，抓取第二次/v1/chat/completions的messages。

**期望：** 获得授权历史、技能约束、本次/已生成报告的数据来源、关键统计和缺口摘要；未提供时明确不能解释该报告。

**实际：** 第二次已有ALPHA历史和skill name/summary，但仅system+历史对话；system写Dataset:unbound，没有factor-momentum.csv、IC数值、截面数、gaps、产物摘要或SKILL约束正文。local分析先完成但未进入请求。mock不回显本地报告，测试没有把模型猜对冒充上下文通过。

**修改建议：** 构建有预算的typed context envelope：dataset id/hash、统计、gaps、绑定技能约束和artifact摘要；不必发整份HTML，但应与右侧一致，隔离资料与指令且不发密钥。

**建议补测：** 抓包断言两轮历史、技能约束、指定数据集、IC/截面摘要和缺口；密钥不进入messages。

证据：L:020-021-model-capture。


## 一般（P2）

### ASTRA-20260921-001 窄屏避免裁切的代价是入口说明几乎不可读

- 闸门：设计；严重度：P2；状态：`partial`。
- 位置：`apps/web/src/components/Glass.tsx:59-73,91-112`。

复现：
1. 390x844、浏览器缩放100%打开首页，使用薄雾预设。
2. 观察520px高的扇形区，聚焦首张卡，再点击四个入口确认路由。

**期望：** 完整显示且保持可读；不能只用不越界作为响应式验收。

**实际：** stage350x520、fit0.39371980676328505；22px/11px经缩放约8.66px/4.33px，激活1.07倍后仍很小。截图显示大块空白和一小叠卡。四个入口可点，桌面hover/focus和裁切已修。

**修改建议：** 保留Mosha层，窄屏改独立卡、可滚动卡或列表布局，调整gap/高度/文案，不将文字整体缩到0.39；设置可读字号和点击热区验收下限。

**建议补测：** 390/768/1440真实截图、点击、键盘焦点和reduced-motion；记录变换后尺寸，不只断言scrollWidth。

证据：B:r2-home-390.png,001-home,002-mobile；C:r2-mobile-card-focus.png,001-narrow-readability-and-links。

### ASTRA-20260921-008 CSV剩余无损性与引号语法校验未闭合

- 闸门：数据；严重度：P2；状态：`partial`。
- 位置：`server/lib/studio.mjs:65-158`。

复现：
1. parseCsv处理a,b / "  padded  ",1，检查字段内部空格。
2. 处理a,b / ab"cd",1。
3. 处理合法CSV a,b / "text<b>hi</b>",1。

**期望：** 保留quoted字段内部空格和文本；非法中途引号拒绝；防HTML冒充不能误伤合法CSV字段文字。

**实际：** padded前后空格丢失；非法ab"cd"被接受并改写abcd；合法含<b>文本字段返回invalid-csv。原quoted multiline两条变三条、重复列、未闭合引号和HTML伪装已修；本轮残余降P2，不继续说原P0数值错行未修。

**修改建议：** 使用完整CSV状态机或成熟解析器，quoted值原样保留；限定合法引号位置，文件结构校验不用前400字符的笼统HTML标签正则。

**建议补测：** quoted spaces、逗号/换行/双引号、非法quote-in-unquoted、正常标签文本和真正HTML文件。

证据：L:008-csv,008-extra-csv；B:008-023-csv-ui。

### ASTRA-20260921-029 8000字输入合同与模型4000字裁剪仍不一致

- 闸门：产品；严重度：P2；状态：`partial`。
- 位置：`server/lib/studio.mjs:857-862; apps/web/src/pages/Conversations.tsx composer`。

复现：
1. 接本地mock模型，经API提交4500个A加ASTRA-END-INSTRUCTION，共4521字符。
2. 比较存储user消息与mock收到的当前user.content长度及尾标。
3. 浏览器粘贴8100个A加ASTRA-END，共8109字符，观察输入与提示。

**期望：** 上限内当前输入完整进入处理链；超限明确拒绝或确认裁剪，不在后续阶段无提示删要求。

**实际：** 存储4521，模型当前user只有4000且尾标丢失，history映射对当前消息也slice(0,4000)。UI粘贴8109只留下8000，提示却为已到上限，不会静默截断。服务端8001返回too-long已修。

**修改建议：** 统一输入和模型预算；保留当前消息，裁剪历史要有策略/提示；超限粘贴明确拒绝或提示截去数量，不能承诺不会截断。

**建议补测：** 3999/4000/4001/7999/8000/8001、尾部限制、中文/emoji；比对存储与实际模型payload。

证据：L:029-current-model-truncation,029-long；B:029-long-ui。

### ASTRA-20260921-032 关闭比赛后确认仍可点，后端拒绝被界面吞掉

- 闸门：产品；严重度：P2；状态：`open`。
- 位置：`apps/web/src/pages/Competitions.tsx:20-27,140-159`。

复现：
1. 启用模拟赛并创建pending计划。
2. 比赛页点击关闭比赛模式。
3. 点击计划行确认执行这笔交易，观察Network、页面与fills。

**期望：** offline禁用确认并说明原因，或显示错误和重新连接入口；不能只在控制台异常。

**实际：** 真实Chrome：confirm400 contest-offline，pageerror出现，但qs-error/alert为空，pending和按钮保留。fills0→0。没有未确认成交，不推翻016后端门通过或023消息/导入路径已修。

**修改建议：** 确认/取消/开始等mutation捕获并呈现错误，离线禁用确认，提供重新连接后重试，失败按钮恢复。

**建议补测：** offline/expired/余额不足/503/重复点击；可见错误、按钮状态正确、fills不增。

证据：C:032-offline-confirm-error,r2-offline-confirm-error.png。

### ASTRA-20260921-028 依赖告警仍在，延期不等于安全门通过

- 闸门：安全；严重度：P2；状态：`deferred`。
- 位置：`package.json; apps/web/package.json; npm audit`。

复现：
1. 冻结SHA干净目录npm install。
2. 运行npm audit --json和npm audit --prefix apps/web --json，逐条核对公告与实际调用模式。

**期望：** 保留未修告警和可达性范围，另排升级/回归；扫描等级不是实际RCE证明，尚未证明RCE也不是永久豁免。

**实际：** root critical2包（concurrently→shell-quote），web high2包（react-router-dom→react-router），各链含多条公告。npm建议concurrently9.2.4、react-router-dom7.18.4。本应用BrowserRouter；部分Framework/SSR专属问题不直接适用，但不能据此排除所有其他公告。

**修改建议：** 继续deferred，明确负责人/截止条件；单独升级、追踪输入可达性并回归dev/路由/构建，勿写已证明RCE或全部不适用。

**建议补测：** 升级后audit、npm run dev、build、路由/前后退/刷新/外链。

证据：A:npm-audit-root.json,npm-audit-web.json。


## 建议（P3）

### ASTRA-20260921-031 锁文件和dist忽略仍延期，端口说明已改善

- 闸门：后端；严重度：P3；状态：`deferred`。
- 位置：`apps/web/package-lock.json; .gitignore; server/index.mjs server.on(error)`。

复现：
1. 干净目录npm install后git status --short。
2. 核对dist忽略和重复占用同端口启动。

**期望：** 安装可重复、构建产物不误提交、启动故障可诊断。

**实际：** 安装仍改写web lockfile，dist仍未跟踪而非忽略。重复端口已有友好Port 3198 is already in use说明并exit1，应承认此子项已修。

**修改建议：** 提交完整web锁文件并npm ci验证，补dist忽略；保留已改好的端口提示，不重复修通过的子项。

**建议补测：** 干净安装构建状态、npm ci、未build 503提示、最低与不支持Node版本；本轮未独立执行低于支持版本的Node。

证据：A:install-working-tree.txt；L:port-in-use。

## 数据独立对照

独立Python csv/statistics/math，不导入产品分析代码；逐日80个IC与产品逐一比较，容差1e-12内一致。

| 文件 | 独立结果 |
|---|---|
| csi300-daily.csv | 420行；文件末日2025-08-11；close4226.65；最大回撤-0.04595874922021215，与产品一致。 |
| factor-momentum.csv | 640行、80日截面；Pearson IC均值0.935314813971839，样本标准差0.027587113467268488，与产品浮点误差范围内一致。 |

仅仓库seed文件计算一致性，不是真实行情来源背书、实时行情认证或策略胜率。空表/缺口和研究免责声明亦检查。

## Mosha固定参考对照

[固定参考](https://github.com/Simon66-workshop/mosha-card-ui/tree/7abd3b0b811f483f8d172f15e843836496824b3f)；实际打开demo/cards.html并悬停。
8个Mosha复制模块/CSS与固定参考字节一致，包括mosha-card.css、defaults.ts、recipe.ts。
#07080c; Sora/Syne/Fraunces; 原片/薄雾/夜虹/厚玻璃/极简/液态/透镜; sheen/rim/grain/chroma/depth保留。
reduced-motion实测transition=0s、animation=none。七预设已改变渲染；001的窄屏字号是容器集成问题，不是要求另换UI框架。

## 声明缺口核对

- **DSH/vendor/PandaData/official contest/TypeSafe absent：agree。** 声明缺口仍在，不重写为新P0。
- **compact expert/team metadata：agree。** UI现有缺口说明，不代表内容已补全。
- **8second model fallback：agree-after-fix。** 020路径与失败轨迹通过本地mock验证，不是实际provider认证。
- **shared member isolation：agree。** 仍非共享部署验收；005是默认本地其他来源的独立反例，不是借已声明缺口重复开P0。
- **cursor bot push403：historical-not-retested。** 不对Cursor当前凭据/权限作新断言。

## 证据入口

A/B/C截图、浏览器脚本、日志artifacts保留至2026-12-20（管理者删除或调整除外）；三个ZIP均已下载并核验SHA256。与关键结论相关的截图已打开核对，没有将缺字字体/管理策略阻断记作产品bug。

| 代号 | GitHub运行 / artifact | ZIP SHA-256 |
|---|---|---|
| A | [run 35609235743](https://github.com/Simon66-workshop/66QuantStudio/actions/runs/35609235743)；`astra-20260921-r2-baseline` / `10642094207` | `d7fd89bac210f1735888a843251cddcf3b6496cc405005cd9b3dc1fce4e782ff` |
| B | [run 35610462012](https://github.com/Simon66-workshop/66QuantStudio/actions/runs/35610462012)；`astra-20260921-r2-browser` / `10644090956` | `643daaae42cdb45fd728f756d59538ee1f13aee73ac122f332e9a59f0cfad895` |
| C | [run 35611463042](https://github.com/Simon66-workshop/66QuantStudio/actions/runs/35611463042)；`astra-20260921-r2-followup` / `10644780171` | `6d8603861af0993bf68112d46a3be8a5844738fc75bad88d42f05e76aff25aee` |

Astra独立本地Node/HTTP合成fixtures，不是Cursor日志；永久报告保留关键数值和可重现步骤，不要求聊天附件。
L关键数值、模型消息摘录和截断计数已收录同名JSON的evidence.L；不要求取得本地工作目录。该摘要不替代各finding的复现步骤。
优先看：B的r2-home-390.png、r2-menu-390.png、r2-factor-report.png；C的r2-mobile-card-focus.png、r2-mosha-reference-hover.png、r2-negative-quantity-pending.png、r2-close-short-long-reduced.png、r2-offline-confirm-error.png。
源码位置均指[冻结SHA](https://github.com/Simon66-workshop/66QuantStudio/tree/a0a1213bad9df74aa561ea2a8e2814b987fc2137)，不随main移动而静默更换。

外部技术依据仅限定合同/可达性，不代替本次实测：
- [§2 CSV空格与引号规则，支持008残余。](https://www.rfc-editor.org/rfc/rfc4180)
- [shell-quote公告需要实际危险调用链，扫描不等于本应用RCE。](https://github.com/advisories/GHSA-w7jw-789q-3m8p)
- [该公告明确区分Declarative BrowserRouter与受影响模式，不用它排除其他不同公告。](https://github.com/advisories/GHSA-2w69-qvjg-hvjx)

## 给Cursor的下一轮最小回归

按005→015/014→021/029→001/032→008修；028/031单独排期并写负责人/条件。005必须真实其他来源，015必须方向账本，021/029必须抓mock实际payload，001必须390px可读截图，032必须可见错误且fills不增。其余22条做必要保护回归，不从零重写同义清单。

每个修复继续按原ID回复：commit、反例前后结果、真实测试环境。延期不能改成fixed，已有22条只做必要保护，不从零生成同义清单。

## 未执行/不据此放行

- iOS Safari/Android真机
- 真实PandaData/官方Jev/官方赛事成交/模型质量
- 操作系统断电和多进程持久化故障注入
- 低于支持版本的Node
- 每条依赖公告的完整利用验证
- 生产或共享部署验收

这些边界不是本轮已复现反例的替代解释。未修改产品代码，未合并main。
