# Astra 审计 2026-09-21

- 仓库：https://github.com/Simon66-workshop/66QuantStudio
- 固定 main SHA：`ac3b8915488da0664d78a4b19afb262636735bbe`。所有产品代码位置均指此版本。
- Mosha 对照：`Simon66-workshop/mosha-card-ui@7abd3b0b811f483f8d172f15e843836496824b3f`。
- 环境：GitHub Actions Ubuntu Linux，Node 22.16.0，npm 10.9.2，Chrome 152.0.7977.82，Playwright 1.55.1；视口 1440×1000 / 390×844 / 768×844。
- 原始命令：真实 clone → npm install → npm test（6/6）→ npm run build → npm start，均成功；真实浏览器打开并点击 http://127.0.0.1:3198/。
- 结论：**FAIL，不能把“可启动、6/6”当作可交付验收通过。**
- Findings：31 项；P0=7，P1=12，P2=11，P3=1。

## 先看打开就能发现的问题

首页扇形卡底边被裁切，透明叠层把相邻文字叠在一起，悬停又不能像 Mosha 参考一样把目标卡单独提出来（001）。窄屏直接藏掉整个导航，却没有移动菜单，数据库和设置等栏目因此没有可用入口（002）。设置的七个玻璃预设看似保存成功，回到首页却始终是同一套效果（003）。进入会话后，“发送”两个字还被挤成上下两行（004）。这些不是用户操作错，也不是缺 DSH、PandaData 或 TypeSafe 凭证；在本次干净安装中即可复现。进一步测试还发现跨来源写入/确认、畸形请求导致进程退出、并发状态损坏和错误统计，故五条闸门均不能给整体 PASS。

| 首屏问题 | 实测证据 |
|---|---|
| 001 卡片裁切与透叠 | B / desktop-home.png、mobile-390.png；C / mosha-reference-hover.png |
| 002 移动导航消失 | B / mobile-390.png、mobile-768.png |
| 003 预设无渲染变化 | B / browser-results.json / 05-preset-controls |
| 004 发送按钮竖排 | B / factor-report.png；C / composer-squeezed-label |

## 证据、运行边界与复现入口

审计容器无法解析 GitHub 域名，且本机 Chromium 导航被管理员策略阻断；真实 clone/install/test/build/start/浏览器点击在隔离 GitHub Actions 执行，另在下载的固定 Git bundle 上复测 Node/API/算法。没有绕过本机浏览器策略。

A 是首次启动和命令证据；B 是有中文字库后的完整 UI 扫描；C 是遗漏控件补测、Mosha 实际对照和独立算法/并发探针。**审计步骤 completed 或 Actions success 只表示探针完成，不表示被测产品通过。** 首轮收藏使用了错误的测试选择器，C 已改用实际“收藏/取消”按钮补测通过；不把该超时当产品缺陷。C 中专家/团队截图抓取过早仍显示列表，不用这些截图证明详情页坏；实际后退、前进、刷新和专属/团队开会话动作已执行。

### 证据包 A
- Run：[GitHub Actions 35597053327](https://github.com/Simon66-workshop/66QuantStudio/actions/runs/35597053327)
- Artifact：`astra-20260921-baseline`，ID `10636813697`，到期 `2026-09-23`。
- ZIP SHA-256：`3ac20583b70f6138b00b8bc2738ec72c39af4ae616e68088e44a98e5d0e6abc1`。

### 证据包 B
- Run：[GitHub Actions 35597708321](https://github.com/Simon66-workshop/66QuantStudio/actions/runs/35597708321)
- Artifact：`astra-20260921-browser`，ID `10637373824`，到期 `2026-10-21`。
- ZIP SHA-256：`c949e07b62a06a5a6227315551af5271b6be68707fb12350aab8bcf5cd9f02d4`。
- 冻结审计脚本：[ASTRA-20260921-000.mjs](https://github.com/Simon66-workshop/66QuantStudio/blob/1ca8bf4f5433bcdce816103df622b3c52e935451/docs/audits/ASTRA-20260921-000.mjs)。

### 证据包 C
- Run：[GitHub Actions 35598608939](https://github.com/Simon66-workshop/66QuantStudio/actions/runs/35598608939)
- Artifact：`astra-20260921-followup`，ID `10637762008`，到期 `2026-10-21`。
- ZIP SHA-256：`6c952e72efeb0a7f114dcb582cbaf772dd3d2a56576648408e0f91f60b2aacf3`。
- 冻结审计脚本：[ASTRA-20260921-000.mjs](https://github.com/Simon66-workshop/66QuantStudio/blob/907766faa721c9adee212bcc454ded9a851e44fa/docs/audits/ASTRA-20260921-000.mjs)。

截图/log/结果 JSON 位于上述 artifact，B/C 含 SHA256SUMS.txt。Artifact 有期限；不可把临时下载地址当永久证据，脚本和本报告中的输入/输出可在固定版本重新复现。本地补充 L 的原始文件未额外推入仓库，以下有关 L 的条目给出具体输入、进程结果或计算值，不伪造仓库文件链接。

本轮最终提交相对固定 main 的净差异只有这份 Markdown 与同名 JSON；产品代码没有修复。临时审计 workflow/runner 已从最终树移除，其历史 SHA 用于证据追溯；不改 docs/task-report.md，不提交依赖、dist、密钥或 vendor。

### 五条闸门结果

| 闸门 | 结论 | 已成立与未成立的边界 |
|---|---|
| 产品 | FAIL | 四入口、基本会话/报告、导入及模拟确认流程可运行；上传文件选择、模型连续上下文、移动导航等主路径坏。 |
| 设计 | FAIL | 真正复用了 Mosha 光学层，不是另换皮肤；响应式、聚焦交互和设置消费不完整。 |
| 后端 | FAIL | health/构建/原有单测通过；并发持久化、计划状态机和账本不可靠。 |
| 安全 | FAIL | 转义、CSP、nosniff、密钥脱敏等部分成立；跨 Origin 写入和单请求进程崩溃是新问题。 |
| 数据 | FAIL | 种子算术可独立复算；合法 CSV、缺数、零方差和排序边界会产生错误或不可见缺口。 |

## 阻塞（P0）

### ASTRA-20260921-005 另一网页来源可读取工作区、改设置并确认模拟交易

- 闸门：安全；严重度：**P0**；状态：`open`。
- 位置：`server/index.mjs:10,27-40,97-102; POST /api/contest/plans/:id/confirm`。

**复现**

1. 只在隔离演示实例创建一笔pending模拟计划。
2. 从http://127.0.0.1:3201页面fetch 3198/api/bootstrap。
3. 同一页面PUT设置，再POST该计划的confirm，不点击工作台确认按钮。

**期望：** 未获授权的Origin不能读取私有工作区或执行写入/确认。

**实际：** 真实Chrome跨origin读取200、写入200、确认200，fills从1变2；默认监听0.0.0.0且API允许CORS *，没有应用鉴权/Origin门。证明范围是另一loopback网页来源，不是对所有互联网网页可利用性的证明；全部为合成模拟交易。

**修改建议：** 本机默认绑定loopback；校验Host/Origin；移除宽泛CORS；写入与确认使用不可由任意来源获取的会话授权/CSRF防护。共享部署另设认证和隔离。

**建议补测试：** 真实浏览器可信/非可信Origin，GET/预检/PUT/confirm；非可信请求前后fills和设置不变。

**证据：** B:18-cross-origin-read-write-confirm。

### ASTRA-20260921-006 一个畸形Host请求可使整个Node服务退出

- 闸门：安全；严重度：**P0**；状态：`open`。
- 位置：`server/index.mjs:97-103`。

**复现**

1. 将server目录复制到临时目录，以PORT=3212 HOST=127.0.0.1启动。
2. 发送原始HTTP请求：GET /api/health HTTP/1.1，Host头值为左方括号[。
3. 检查进程退出码并再次请求health。

**期望：** 返回400，进程继续服务正常请求。

**实际：** new URL位于try之外，抛ERR_INVALID_URL；连接无响应，进程exit=1。另一个畸形百分号路径返回500而非客户端错误。未对用户生产进程发送此探针。

**修改建议：** 把URL解析和Host验证纳入受控错误边界，解析不依赖任意Host，非法Host/URI返回400；避免请求级异常变成未处理拒绝。

**建议补测试：** raw HTTP畸形Host、非法percent、正常请求交错；进程持续存活。

**证据：** L:api-results.json malformed-host-process-exit=1,malformed-host-log。

### ASTRA-20260921-007 并发保存会损坏state.json，重启静默丢失已成功保存的数据

- 闸门：后端；严重度：**P0**；状态：`open`。
- 位置：`server/lib/studio.mjs:294-306`。

**复现**

1. 用createStudio在临时数据目录初始化。
2. Promise.allSettled并发创建24个名称唯一的技能，步骤长度略有差异。
3. 读取并JSON.parse state.json，再用createStudio重启；重复5轮。

**期望：** 全部成功响应的数据持久化一致；损坏状态不能自动覆盖为默认空工作区。

**实际：** C中5轮有2轮磁盘JSON损坏，24个Promise均fulfilled，内存技能39，重启只剩种子15；本地另5轮复现1轮。共享PID临时文件加非原子覆盖发生竞争；catch吞掉损坏并重置。复现频率不是生产发生概率。

**修改建议：** 串行化/事务化状态写入；唯一临时文件加原子rename和必要fsync，失败不回成功；保留损坏文件并显式恢复，禁止静默重置。

**建议补测试：** 并发创建/消息/设置、写失败和重启；所有已确认写入完整保留，JSON始终可解析。

**证据：** C:concurrent-write-and-restart；L:engine-results.json concurrent-persistence-corruption。

### ASTRA-20260921-008 合法CSV引号内换行被拆成多行，非法CSV又被当成功导入

- 闸门：数据；严重度：**P0**；状态：`open`。
- 位置：`server/lib/studio.mjs:55-89,648-665`。

**复现**

1. 导入date,note,close三列，第一行note为带引号的first换行second，close=100；第二行close=110。
2. 再导入重复列名、未闭合引号及内容为<html>not a CSV</html>的.csv文件。

**期望：** 合法两条记录保持两条，字段值无损；不合法格式明确拒绝或给出结构错误。

**实际：** 浏览器实际将两条记录变为三条，100丢入错误字段且价格空缺；HTML文本成为单列零行数据集。重复列名覆盖值、未闭合引号也接受。

**修改建议：** 使用支持quoted multiline的成熟CSV解析器；验证列名唯一、行宽、引号闭合和预期结构，导入前提供预览与错误定位。

**建议补测试：** 引号内逗号/换行/双引号、BOM、CRLF、重复列名、坏引号、非CSV文本。

**证据：** B:09-csv-import,csv-multiline.png；L:engine-results.json csv-parser。

### ASTRA-20260921-009 缺失价格被转成0，报告制造出-100%最大回撤

- 闸门：数据；严重度：**P0**；状态：`open`。
- 位置：`server/lib/studio.mjs:92-95,162-196`。

**复现**

1. 调用analyzeTable，date顺序正确，close依次为100、空字符串、110。
2. 检查closeStats、gaps及HTML。

**期望：** 空值是缺失而非真实零；按明确策略处理，并呈现覆盖缺口。

**实际：** n=3、min=0、maxDrawdown=-1、gaps=[]。原因是Number('')=0，报告把缺数当成暴跌。

**修改建议：** 数值解析前识别空白/null/缺失及非法价格；保留行级有效性和缺失计数，禁止默默填0。

**建议补测试：** 空白、null、NaN、真实零、负价和断点策略；100/缺失/110不输出伪造-100%。

**证据：** C:missing-price-and-unsorted-date；L:engine-results.json blank-price-coerced-zero。

### ASTRA-20260921-010 零方差截面的Pearson IC被伪造为有效的0

- 闸门：数据；严重度：**P0**；状态：`open`。
- 位置：`server/lib/studio.mjs:199-238`。

**复现**

1. 准备8个日期，每日4个不同symbol，factor全为1，forward依次0/.01/.02/.03。
2. 调用evaluateFactor并检查每个IC、gaps和HTML。

**期望：** 分母为零的相关系数未定义，应排除并标zero-variance等缺口；无有效截面不能报告有效IC均值。

**实际：** 8个IC均为0，gaps=[]，HTML写截面8、均值0、IR0。Math.sqrt(fd*rd)||1把无定义统计量变成正常结果。

**修改建议：** 显式检查有效样本和两侧方差；无定义值用null及原因，汇总只用有效截面，零标准差IR亦标未定义。

**建议补测试：** 常数因子、常数收益、空白因子、重复symbol和正常截面；与独立Pearson计算对照。

**证据：** C:ic-undefined-and-hidden-gap。

### ASTRA-20260921-015 模拟成交账本不守恒，两次开多只有一手仓位，开空有fill无仓位

- 闸门：后端；严重度：**P0**；状态：`open`。
- 位置：`server/lib/studio.mjs:735-767`。

**复现**

1. 新隔离账户连续创建并确认两笔一手开多。
2. 核对fills、positions、margin、available、riskRatio。
3. 另用合成行情触发Jev open-short并确认。

**期望：** 成交、持仓、保证金和风险指标按同一记账规则一致更新；未实现的方向拒绝执行。

**实际：** 两笔long fill但positions只有1手；margin=204000、available=796000而riskRatio仍0.18。open-short产生fill却仍flat/0手。仅模拟账本，未触及真实账户。

**修改建议：** 建立每合约/方向的确定性账本和不变量校验；累加/减仓正确，拒绝无仓平仓，所有支持动作一致更新账户与回执。

**建议补测试：** 两次加仓、部分/全部平仓、空仓平仓、多空、多个合约、重复确认；账本可由fills重放得到。

**证据：** C:ledger-expiry-and-trade-intent,jev-breakout-repeat-stop-short-ledger。

## 严重（P1）

### ASTRA-20260921-002 900px以下所有侧栏导航消失，没有移动端替代入口

- 闸门：产品；严重度：**P1**；状态：`open`。
- 位置：`apps/web/src/styles.css:143-150; apps/web/src/components/Shell.tsx`。

**复现**

1. 以390px或768px打开首页。
2. 寻找数据库、设置、收藏等导航，再进入一个会话尝试回到这些页面。

**期望：** 窄屏仍可通过菜单或底部导航访问全部栏目。

**实际：** navVisible=false；.qs-nav直接display:none，没有菜单按钮。首页四卡不能替代全部栏目。

**修改建议：** 实现同一路由清单的移动端菜单，保留当前位置、关闭和键盘操作；不要仅隐藏侧栏。

**建议补测试：** 390/768/900/901px逐项导航，进入会话后可返回主页和设置。

**证据：** B:03-mobile；B:mobile-390.png,mobile-768.png。

### ASTRA-20260921-011 最新值和最大回撤按输入行顺序计算，没有时间顺序契约

- 闸门：数据；严重度：**P1**；状态：`open`。
- 位置：`server/lib/studio.mjs:98-118,162-196`。

**复现**

1. 输入日期/价格为01-03/110、01-01/100、01-02/90。
2. 查看报告的最新和最大回撤。

**期望：** 识别并验证日期顺序，排序或拒绝无序输入；不能把最后一行直接称最新。

**实际：** 最新90、回撤-18.18%；按日期排列应最新110、回撤-10%。没有排序或缺口提示。

**修改建议：** 明确定义时间列和单一资产范围，验证重复/无序日期；排序后计算，并记录排序及缺失策略。

**建议补测试：** 同一时间序列多种行顺序、重复日期、多资产混表；不能产生顺序依赖的伪最新。

**证据：** C:missing-price-and-unsorted-date。

### ASTRA-20260921-012 截面不足只存在JSON，HTML反而声称数据不足已标注

- 闸门：数据；严重度：**P1**；状态：`open`。
- 位置：`server/lib/studio.mjs:227-238`。

**复现**

1. 传入一个日期、4个有效symbol，因子与收益线性相关。
2. 同时读取evaluateFactor.gaps和返回HTML。

**期望：** 1至7个有效截面的不足提示在报告正文醒目呈现。

**实际：** JSON有insufficient-cross-sections，但st存在时HTML只输出IC摘要，没有该缺口；页脚仍写数据不足已标注。

**修改建议：** 让结构化gaps统一驱动报告/会话/UI，在有统计值时也显示；样本过少不要输出貌似完整的IR。

**建议补测试：** 0、1、7、8截面；JSON与HTML缺口逐项一致。

**证据：** C:ic-undefined-and-hidden-gap。

### ASTRA-20260921-013 明确指定上传文件仍分析种子表，price还被IC子串误路由

- 闸门：产品；严重度：**P1**；状态：`open`。
- 位置：`server/lib/studio.mjs:474-495; apps/web/src/pages/Database.tsx`。

**复现**

1. 导入astra-price.csv，三日close=100/120/90。
2. 新建普通会话，发送只分析我刚上传的astra-price.csv，告诉我最新收盘和最大回撤。

**期望：** 绑定该上传datasetId，返回最新90和回撤-25%，并显示所用数据集。

**实际：** 返回factor-momentum.csv的IC均值0.9353/80截面，而非上传文件；/IC/i还匹配price中的ic。即使不触发因子路由，代码仍优先硬选csi300种子。

**修改建议：** 会话显式绑定datasetId并校验；自然语言匹配只能建议候选，不能覆盖用户指定；IC用词边界而非任意子串。

**建议补测试：** 上传文件端到端分析，含price等文件名；替换文件后报告数据源和结果同步变化。

**证据：** C:requested-upload-ignored。

### ASTRA-20260921-014 交易意图被硬编码，开空变开多、撤单变平仓

- 闸门：产品；严重度：**P1**；状态：`open`。
- 位置：`server/lib/studio.mjs:450-467`。

**复现**

1. 在模拟赛会话分别发开多rb2610 3手、开空cu2701 5手、撤单rb2610。
2. 只查看pending计划，不实际确认。

**期望：** 支持的意图准确提取合约/方向/数量；不支持的指令拒绝或澄清，撤单不是新建平仓单。

**实际：** 数量总为1、合约总为rb2610；开空cu2701变rb2610 open-long；撤单变close。人工确认门存在，但计划本身错误。

**修改建议：** 用受约束交易意图结构和确定性校验；未知合约/数量/动作不得默认下单方向；撤单关联已有order/plan。

**建议补测试：** 多/空/平/撤、不同合约数量、否定句、含糊指令；确认前仍fills=0。

**证据：** C:ledger-expiry-and-trade-intent；L:engine-results.json trade-parser-hardcoded。

### ASTRA-20260921-016 过期或已断开账户的pending计划仍可确认成交

- 闸门：后端；严重度：**P1**；状态：`open`。
- 位置：`server/lib/studio.mjs:722-767`。

**复现**

1. 隔离fixture创建pending计划，将其expiresAt设为过去时间。
2. 将模拟赛enabled/connected设false，再confirm该计划。

**期望：** 服务端拒绝过期、断开或关闭状态的执行请求，fills不增加。

**实际：** 只检查status=pending；过期时间为2000年的计划在账户关闭后仍executed-sim并生成fill。过期场景用固定fixture而非等待15分钟。

**修改建议：** 确认时重新校验过期、账户连接、业务开关和风险边界；状态变更与记账同一事务。

**建议补测试：** 过期边界、断开、关闭、取消后确认、重复确认、重启后旧计划。

**证据：** C:ledger-expiry-and-trade-intent。

### ASTRA-20260921-017 突破跟随条件数学上不可能成立

- 闸门：后端；严重度：**P1**；状态：`open`。
- 位置：`server/lib/studio.mjs:799-817`。

**复现**

1. 隔离rb-minutes fixture设前30个last=100，最新last=130，账户无多仓。
2. 启动breakout模板。

**期望：** 最新值高于此前窗口高点时进入突破分支，仍只生成待确认计划。

**实际：** action=hold、plans=[]；比较式last>Math.max(历史值,last)恒不成立。

**修改建议：** 高点窗口明确排除当前bar，校验最小窗口长度和有效行情，不使用含自身的最大值比较。

**建议补测试：** 高于/等于/低于历史高点、窗口不足、缺失值；确认门保持。

**证据：** C:jev-breakout-repeat-stop-short-ledger。

### ASTRA-20260921-019 重新进入模型设置显示空值，再保存会清空有效URL和模型名

- 闸门：产品；严重度：**P1**；状态：`open`。
- 位置：`apps/web/src/pages/Settings.tsx:11-15,63-70; server/lib/studio.mjs:564-571`。

**复现**

1. 保存模拟模型Base URL、model和合成密钥。
2. 离开设置后重新进入，直接点击保存模型服务。

**期望：** 非秘密配置回填；未编辑字段保持原值；空密钥输入不意外更改密钥。

**实际：** URL/model两个输入框为空，再保存后服务器两字段变空，模型停止被调用。密钥仍保留hasKey=true，本审计没有把它误报为密钥被清空。

**修改建议：** 从服务端状态初始化并同步非秘密字段；提交dirty字段；明确保存反馈与独立清除密钥操作。

**建议补测试：** 保存、换页、刷新、只改model、不改直接保存、替换/清除密钥。

**证据：** B:16-model-settings-and-endpoint。

### ASTRA-20260921-020 模型Base URL路径被丢弃，回退原因也不进入消息轨迹

- 闸门：后端；严重度：**P1**；状态：`open`。
- 位置：`server/lib/studio.mjs:516-547,621-630`。

**复现**

1. 设置模拟服务Base URL为http://127.0.0.1:3201/v1，监听请求路径。
2. 发送会话；另让模拟端点返回非200或超时。

**期望：** 尊重/v1等路径前缀；失败回退时UI明确实际执行引擎和原因。

**实际：** 实际POST /chat/completions而不是/v1/chat/completions。非200静默返回local，catch产生modelGap也未写入assistant。new URL还在try外，非法URL不能按预期回退。

**修改建议：** 按规范化base路径拼接端点并在保存时验证URL；统一可见engine/status/error元数据；解析/超时也纳入回退边界。

**建议补测试：** 带/不带路径与尾斜杠、非法URL、401/404/500、8秒超时，断言轨迹标明回退。

**证据：** B:16-model-settings-and-endpoint requests；server/lib/studio.mjs:516-547,621-630。

### ASTRA-20260921-021 模型看不到对话历史、绑定技能和已生成报告

- 闸门：后端；严重度：**P1**；状态：`open`。
- 位置：`server/lib/studio.mjs:516-535`。

**复现**

1. 在绑定factor技能的会话接入本地模拟模型。
2. 先发送记住代号ALPHA，再问刚才的代号。
3. 检查第二次服务端收到的messages。

**期望：** 在安全长度预算内提供历史和被明确授权的技能/数据上下文，否则不能宣称连续专属会话能力。

**实际：** 每次只有固定system和当前user两条；第二次没有ALPHA、历史、技能正文或本地分析结果。右侧报告与模型回答可完全脱节。

**修改建议：** 定义最小会话上下文协议，加入历史/绑定能力/数据来源和摘要，限制token与提示注入边界；不发送密钥。

**建议补测试：** 两轮记忆、技能绑定、指定数据集、上下文超限与秘密不外发断言；用mock检查完整payload。

**证据：** B:16-model-settings-and-endpoint requests。

### ASTRA-20260921-022 Ctrl/Command+Enter绕过忙碌按钮，重复发送同一任务

- 闸门：产品；严重度：**P1**；状态：`open`。
- 位置：`apps/web/src/pages/Conversations.tsx:49-61,93-98; server/lib/studio.mjs:610-635`。

**复现**

1. 模拟模型延迟650ms，输入一次文本。
2. 连续三次Control+Enter，不重新填写文本。

**期望：** 一个进行中的提交最多产生一条用户消息和一份任务；重复事件不重复付费/记账。

**实际：** 同一文本存入3条user消息，生成3份artifact；只禁用按钮，send和快捷键没有in-flight锁，服务器没有幂等键。

**修改建议：** 入口统一检查同步in-flight锁并preventDefault；服务端按请求幂等键去重，允许明确的新任务。

**建议补测试：** 按钮连点、Ctrl/Meta+Enter、网络重试、失败后重试，分别断言消息/产物数量。

**证据：** B:17-duplicate-send-shortcut。

### ASTRA-20260921-023 发送和CSV导入失败只留控制台异常，界面没有可恢复错误

- 闸门：产品；严重度：**P1**；状态：`open`。
- 位置：`apps/web/src/pages/Conversations.tsx:49-61; apps/web/src/pages/Database.tsx:21-27; apps/web/src/pages/Catalog.tsx`。

**复现**

1. 浏览器拦截一次messages请求返回503/ASTRA_TEST_UNAVAILABLE。
2. 另导入空CSV触发400 invalid-csv。
3. 访问不存在的会话ID。

**期望：** 当前操作显示清晰错误、保留输入并允许重试；不存在资源显示404状态，禁止继续向无效资源提交。

**实际：** 发送/CSV均出现pageerror但没有页面错误提示；无效会话显示空会话和可用发送框。初始bootstrap失败倒有可见提示，此处未一概误报。

**修改建议：** 统一mutation错误呈现及重试，区分loading/empty/not-found/error；失败不显示成功状态。

**建议补测试：** 400/404/413/500/503/断网，确认输入保留、按钮复位和可见错误。

**证据：** B:09-csv-import,14-missing-and-error-state,19-bootstrap-loading-failure；B:silent-request-error.png。

## 一般（P2）

### ASTRA-20260921-001 首页扇形卡裁切、文字互相透叠，未接入参考组件的聚焦交互

- 闸门：设计；严重度：**P2**；状态：`open`。
- 位置：`apps/web/src/components/Glass.tsx:46-86`。

**复现**

1. 1440x1000打开首页，观察扇形卡底部并悬停第一张卡。
2. 缩至390px，观察左右两端卡片和标题。
3. 对照固定Mosha提交的demo/cards.html并悬停卡片。

**期望：** 卡片完整可读；窄屏自适应；悬停或键盘聚焦能将目标卡独立展示。

**实际：** 桌面stage底边556.5px，边卡底边574.08px，约17.58px被裁；390px下卡片越出stage。产品悬停后仍是mosha-hand，参考为mosha-hand is-isolating。四个入口实际均能点击，问题不是全体按钮失效。

**修改建议：** 沿用Mosha完整交互/响应式导出组件，按容器测算缩放与高度；不要只复制静态光学层和固定scale(0.92)。

**建议补测试：** 桌面/390/768截图；四入口鼠标与键盘；hover/focus可读；reduced-motion。

**证据：** B:01-route-sweep,03-mobile,04-home-clipping-hover-focus；B:desktop-home.png,mobile-390.png；C:mosha-reference-actual-render,mosha-reference-hover.png。

### ASTRA-20260921-003 七套预设和动态背景开关只保存状态，未改变实际渲染

- 闸门：设计；严重度：**P2**；状态：`open`。
- 位置：`apps/web/src/pages/Home.tsx:33; apps/web/src/pages/Settings.tsx:35-58; apps/web/src/styles.css:106-142`。

**复现**

1. 设置依次点击原片、薄雾、夜虹、厚玻璃、极简、液态、透镜。
2. 每次返回首页读取stage样式，切换动态背景后再次观察。

**期望：** 保存的预设应用到卡片，动态开关控制实际效果。

**实际：** 七次API值正确变化，但stage样式完全相同，始终mist参数；shell始终blur=22px/fill=.22。Home硬编码preset=mist；animatedBg没有渲染消费。

**修改建议：** 从settings驱动Mosha参数和背景层；为不支持的设置明确禁用，勿展示无效控制。

**建议补测试：** 至少原片/厚玻璃/透镜的计算样式应不同，刷新后保持；背景开关有实际DOM或动画差异。

**证据：** B:05-preset-controls。

### ASTRA-20260921-004 会话发送按钮被输入框挤成两行竖字

- 闸门：设计；严重度：**P2**；状态：`open`。
- 位置：`apps/web/src/pages/Conversations.tsx:87-99; apps/web/src/styles.css:75-103`。

**复现**

1. 1440x1000打开因子会话，查看输入框右侧发送按钮。

**期望：** 发送/执行中标签完整横排，点击区域稳定。

**实际：** 按钮宽45.78px，左右padding合计24px，余下约21.78px不足两个12px汉字；截图中发送分成上下两行。

**修改建议：** 按钮加shrink-0和whitespace-nowrap，输入框改flex-1 min-w-0；保留足够触控高度。

**建议补测试：** 空会话、报告并列、执行中，以及窄屏截图和按钮尺寸断言。

**证据：** B:factor-report.png；C:composer-squeezed-label。

### ASTRA-20260921-018 盯盘运行中只是一次采样，反复采样又堆积相同待确认计划

- 闸门：产品；严重度：**P2**；状态：`open`。
- 位置：`server/lib/studio.mjs:781-858; apps/web/src/pages/Competitions.tsx:86-123`。

**复现**

1. 点击开始盯盘，观察日志；不点击采样。
2. 在相同合成行情下启动range并再采样两次，最后停止。

**期望：** 状态文案与实际运行方式一致；同一行情/意图未确认时避免重复建议，停止取消该任务的pending计划。

**实际：** 源码无后台调度器，开始只调用一次jevTick；浏览器等待1.4秒状态和时间戳不变，只有点击才新增。相同fixture生成3笔同方向pending；停止确实全部取消。等待值本身不是无调度器的唯一证据。

**修改建议：** 当前先改名为本地单次评估并说明手动采样，或实现有生命周期的调度；按行情/模板/方向去重未确认计划。

**建议补测试：** 相同/新行情重复采样、重启恢复、停止后不再产生计划；明确调度契约。

**证据：** B:13-jev-controls；C:jev-breakout-repeat-stop-short-ledger。

### ASTRA-20260921-024 技能身份与安装状态未形成一致约束

- 闸门：产品；严重度：**P2**；状态：`open`。
- 位置：`server/lib/studio.mjs:331-346,406-418,591-607,687-720; apps/web/src/pages/Catalog.tsx:64-107`。

**复现**

1. 连续创建两个同名本机技能，描述不同。
2. 卸载一个内置技能后打开详情并开始会话。
3. 访问/skills/astra-does-not-exist并点击开始。

**期望：** 技能ID唯一；未知或未安装技能不能假装可执行；有清晰重新安装入口。

**实际：** 两个同名技能得到相同skill-local-astra-同名技能 ID；卸载后installed=false仍可开始，无安装按钮；未知ID也可建会话。

**修改建议：** 不可碰撞ID或明确同名更新语义；后端校验实体存在及安装状态，UI消费同一能力状态。

**建议补测试：** 同名创建、改名/删除、卸载/重装、未知ID以及已有会话绑定。

**证据：** B:10-local-skill-collision,11-uninstall,14-missing-and-error-state。

### ASTRA-20260921-025 新会话列表不刷新，重开会话时右侧报告也未恢复

- 闸门：产品；严重度：**P2**；状态：`open`。
- 位置：`apps/web/src/pages/Conversations.tsx:14-25,42-48,104-116; apps/web/src/pages/Home.tsx:11-16`。

**复现**

1. 新建普通会话后立即点侧栏会话列表。
2. 发送因子需求产出报告，刷新当前会话页面。

**期望：** 列表立即包含刚创建会话；重开时恢复已保存报告或明确选择状态。

**实际：** 服务器会话数从1到2但列表仍只有旧项，刷新才出现。报告刷新后iframe=0，手动点已保存报告按钮后才恢复，报告数据本身未丢失。

**修改建议：** 创建后更新共享store；按conversationId重置/恢复选中artifact，避免过期或空预览。

**建议补测试：** 创建后列表、首页最近会话、刷新、前进后退、会话切换和多报告选择。

**证据：** B:06-new-session-stale-list,07-factor-conversation。

### ASTRA-20260921-026 收藏保存成功却不是可打开的常用入口

- 闸门：产品；严重度：**P2**；状态：`open`。
- 位置：`apps/web/src/pages/Database.tsx:73-104`。

**复现**

1. 点击收藏/取消，刷新页面检查已收藏条目。
2. 尝试从已收藏条目进入技能详情；尝试收藏第五个技能或专家。

**期望：** 收藏条目可打开对应资源，支持实际能力库而非固定前四项。

**实际：** 收藏/取消和持久化通过，但保存列表中a/button数量为0；可收藏候选仅skills.slice(0,4)，技能/专家详情无收藏入口。

**修改建议：** 收藏列表渲染资源链接和取消操作；在相应详情提供统一收藏动作，处理资源不存在。

**建议补测试：** 任意技能/专家收藏、刷新、打开、取消、已删除资源。

**证据：** C:favorites-correct-selector,favorites-saved.png。

### ASTRA-20260921-027 紧凑专家快照缺字段时显示空壳，未把已知限制呈现在使用位置

- 闸门：产品；严重度：**P2**；状态：`open`。
- 位置：`apps/web/src/pages/Catalog.tsx:118-241; server/data/catalog.json`。

**复现**

1. 打开专家和专家团列表，检查职责摘要与LEAD。
2. 读取同一catalog中的excerpt、lead、members字段。

**期望：** 缺失信息明确标为快照未包含，并区分可读名称与已具备的真实协作能力。

**实际：** 专家摘要为空，团队显示LEAD后无姓名；数据缺失与task-report声明一致，但页面没有具体缺口提示。不是把缺少完整DSH或成员元数据再列为新P0。

**修改建议：** 缺字段显示解释与能力边界；补来源元数据后再启用对应信息展示，避免伪造人员/职责。

**建议补测试：** 完整、紧凑、空字段catalog分别渲染；不出现空LEAD或假成员。

**证据：** B:01-route-sweep,desktop-teams.png,desktop-experts.png；docs/task-report.md 已知缺口。

### ASTRA-20260921-028 安装依赖有已公布告警，但不能把扫描级别直接当成本应用可利用RCE

- 闸门：安全；严重度：**P2**；状态：`open`。
- 位置：`package.json; apps/web/package.json; package-lock.json; apps/web/package-lock.json`。

**复现**

1. 在固定基线npm install后分别npm audit --json与npm audit --prefix apps/web --json。
2. 对照公告适用模式和本应用实际调用链。

**期望：** 修复可达依赖风险，或有明确不可达理由和有期限的升级计划。

**实际：** 根树2个critical包计数、web树2个high包计数；主要链为concurrently/shell-quote及react-router-dom/react-router。不是四个独立漏洞。BrowserRouter声明模式不属于GHSA-2w69-qvjg-hvjx所述受影响模式；未证明本应用RCE。

**修改建议：** 优先升级并锁定受支持修复版本，重跑dev/build/路由测试；保留逐条公告的可达性结论，不只删除审计输出。

**建议补测试：** 两层npm audit、依赖树、开发启动与浏览器路由回归。

**证据：** B:npm-audit-root.json,npm-audit-web.json；https://github.com/advisories/GHSA-w7jw-789q-3m8p；https://github.com/advisories/GHSA-2w69-qvjg-hvjx。

### ASTRA-20260921-029 超长研究输入静默截断，用户不知道尾部要求没被处理

- 闸门：产品；严重度：**P2**；状态：`open`。
- 位置：`server/lib/studio.mjs:613; apps/web/src/pages/Conversations.tsx:88-95`。

**复现**

1. 发送8100个A再附加ASTRA-END，合计8109字符。
2. 检查保存的user消息长度和页面警告。

**期望：** 提交前提示长度上限；超限拒绝或明确取得裁剪确认，不静默删掉要求。

**实际：** 存储长度8000、尾部标记丢失，没有超长/截断提示。

**修改建议：** 前后端统一长度契约，显示计数，超限返回可见验证错误；不能只在服务器slice。

**建议补测试：** 7999/8000/8001字符、多字节中文与emoji、尾部限制条件完整性。

**证据：** B:08-empty-long-input。

### ASTRA-20260921-030 不存在的JS资源返回200 HTML，掩盖资源缺失并触发MIME错误

- 闸门：后端；严重度：**P2**；状态：`open`。
- 位置：`server/index.mjs:80-91`。

**复现**

1. 构建并启动后GET /assets/astra-nonexistent.js。
2. 与有效前端路由直接刷新进行对照。

**期望：** 静态资源不存在返回404；仅客户端路由使用HTML fallback。

**实际：** 缺失.js得到200 text/html和首页正文；nosniff下浏览器不能把它当脚本，真实部署旧缓存/缺文件时错误被伪200隐藏。当前正常首页bundle可加载。

**修改建议：** 将/assets等静态命名空间和SPA路由分开；缺失资源404，HTML只对合法导航请求fallback。

**建议补测试：** 有效JS/CSS正确MIME、缺失JS404、会话深链刷新200 HTML。

**证据：** L:api-results.json /assets/astra-nonexistent.js。

## 建议（P3）

### ASTRA-20260921-031 补齐可重复安装和可诊断启动的交付防线

- 闸门：后端；严重度：**P3**；状态：`open`。
- 位置：`apps/web/package-lock.json; .gitignore; server/index.mjs:292-302; package.json`。

**复现**

1. 比较固定仓库web lockfile与npm install后文件；检查dist是否被gitignore。
2. 在隔离目录重复占用同一端口启动；另在没有dist时启动。

**期望：** 完整锁文件与明确构建产物排除规则；常见环境错误给出可操作信息。

**实际：** web lockfile会被安装大幅补写；apps/web/dist不被gitignore但任务明确禁止提交；端口占用仅原始EADDRINUSE堆栈。未build返回503并提示构建，这一项通过。Node低于要求的真实启动未执行。

**修改建议：** 提交完整一致的锁文件并用npm ci验证；忽略dist；添加友好端口/Node版本提示与最小CI。

**建议补测试：** 干净目录安装构建、git status无产物污染、端口占用、未build、最低支持与不支持Node版本。

**证据：** A:install-working-tree.txt；L:api-results.json port-in-use,no-build,gitignore。

## 声明缺口核对

不把上游未集成、没有外部凭证和紧凑快照重复计成新 P0。

### DSH未进入Web进程 — `agree`
package.json与server/index.mjs启动本地服务；不将此列为新P0。

### 无完整vendor/quantstudio — `agree`
固定Git树无完整vendor；scripts/sync-quantstudio.sh存在；本次未将vendor推入仓库。

### 紧凑catalog省略专家/团队字段 — `agree`
原始catalog及UI一致；027仅要求真实呈现限制，不凭缺字段指控新P0。

### 无PandaData/官方赛事CLI，executed-sim — `agree`
浏览器回执和源码明确本地模拟；015批评的是模拟账本算错，不是缺官方连接。

### 无TypeSafe密钥为本地规则 — `agree`
实际source=local-rules，配置路径亦注明not-called；不宣称官方Jev实测。

### 模型8秒超时回退 — `challenge`
定时器8000ms和正常fetch异常回退存在；但new URL在try外、路径被剥离，modelGap/modelUsed未写入消息。不能接受技术方案所称轨迹保留缺口的完整结论，见020。

### 共享部署不隔离成员数据 — `agree`
单一state.json无成员隔离；005是额外的跨Origin写入/确认边界失败，不能由共享说明豁免。

### 历史cursor[bot]403/Contents API写入 — `agree`
作为历史说明接受；本轮未复演其身份，不能推定当前仍403；当前审计连接可写审计分支。

## 已通过的具体检查与不能外推的部分

### 数据独立复算

用 Python csv + statistics 对仓内原始 CSV 独立计算，而不是读取产品声称的数字：CSI300 表 420 行，最后日期 2025-08-11，最后 close=4226.65，按时间顺序运行峰值计算最大回撤 -0.04595874922021215（-4.595874922%）。因子表 640 行，80 个日期截面，每截面 8 行；逐日 Pearson 的均值 0.935314813971839，样本标准差 0.027587113467268488，均值/标准差=33.904047811365615，与基线报告舍入值一致。

这些只是**仓内表的算术一致性**，不是实盘行情真实性认证、因子有效性验证或胜率。无外部行情请求；没有用2025年的末行伪称2026年的实时行情。空表 empty-table 测试通过；研究免责声明存在。零方差、缺数与少截面报告的问题仍见009—012，不能被正常种子样本的正确结果抵消。

### Mosha 逐项对照

在指定参考 SHA 实际打开 demo/cards.html 并悬停截图。产品复制的 store.ts、defaults.ts、symbols.ts、recipe.ts、types.ts、validation.ts、export.ts、mosha-card.css 共8文件与参考逐字节一致；CSS SHA-256 为 `2e93fc1d93ba0a1e0aa060c4f9a9b8bbc17c100beab7ca4a5e1b62c6244ae487`。#07080c 背景、Sora/Syne/Fraunces 字体声明和7个中文预设标签都在。sheen/rim/grain/chroma/depth 光学层确实存在；首次 runner 缺中文字库造成的方框是测试环境问题，未归罪产品。

参考集成约定：[INTEGRATION.md](https://github.com/Simon66-workshop/mosha-card-ui/blob/7abd3b0b811f483f8d172f15e843836496824b3f/docs/INTEGRATION.md)。参考有响应式尺度与交互状态；产品只保留静态扇形和点击导航。reduced-motion 下实测卡片 animation=none、transition=0s，但这不等于完整键盘/触摸/视觉可达性已获认证，更不等于Safari验证。

### 基本产品与安全正例

四张首页卡实际点击后分别进入普通会话、专家团、比赛、技能。目录计数15/44/14成立；这只认证目录计数，不认证15种技能和44种专家均可真实执行。因子技能能打开SKILL.md，发送后右侧HTML能显示；普通空输入不发送。workspace名称保存并刷新有效；收藏切换与持久化有效，不能据此忽略收藏列表不可导航。QUBE/EVO的href、_blank、noreferrer已检查，未嵌入第三方账号；外部站点内部功能没有验收。

正常模拟赛流程：pending时fills=0，确认后fills=1，第二笔取消后fills仍为1；停止能取消未确认Jev计划。没有真实交易、真实模型凭证或付费调用。005证明的是另一loopback网页来源可绕过工作台的交互确认，不把它外推为所有互联网网页均能利用。

合成模型/TypeSafe密钥在GET settings和bootstrap中仅返回hasKey/last4，没有完整值。runtime与.env受gitignore；源码83个跟踪文件和固定基线53个提交的provider-shaped key/private-key标记扫描未命中，这不等于绝对没有任何秘密的保证。转义函数的现有测试通过，报告生成路径做静态核验；未进行穷尽XSS攻击测试。CSP/nosniff存在。编码穿越路径400；明文../先被URL规范化为404，未返回文件，属于与SOP指定400的状态码偏差而非已证明的任意文件读。原型污染形状输入可进入appearance的自有字段，但未证明污染全局原型，不把它报为可利用污染。

### 依赖公告边界

两层npm audit原始JSON留在B。根树concurrently/shell-quote为2个受影响包计数，web树react-router-dom/react-router为2个包计数，不等于4个独立漏洞。核对[GHSA-w7jw-789q-3m8p](https://github.com/advisories/GHSA-w7jw-789q-3m8p)的输入/执行前提；[GHSA-2w69-qvjg-hvjx](https://github.com/advisories/GHSA-2w69-qvjg-hvjx)明确不影响声明式BrowserRouter模式。没有把告警最高级别直接写成本应用已被RCE。修复建议由Cursor做版本升级及可达性复核，不在本审计偷偷更新包。

## 未执行与适用边界

- Safari/iOS/Firefox实机。
- 低于22.12的Node运行。
- 用户实际Cursor云桌面和生产状态。
- 真实PandaData/TypeSafe/赛事柜台/付费模型。
- 外部QUBE/EVO站点内部功能与账号。
- 全互联网来源到loopback的普遍可利用性。
- 大规模压力和所有安全攻击面穷尽。

## 回归范围（给 Cursor）

先保持本次固定基线与findings ID。针对每个ID回复fixed/wontfix/deferred和固定修复SHA；不要用一份总PASS覆盖未修条目。

**第一批：安全与数据保存。** 修005/006后用真实不同Origin和raw HTTP验证拒绝与进程存活；修007后并发写入、落盘失败、重启均不丢已确认数据。暂不开放共享网络，不能把隐藏按钮当服务器授权。

**第二批：分析和模拟账本。** 008—013用正常种子+合法复杂CSV+坏CSV+缺数+无序+零方差+少截面+指定上传文件逐项回归。014—017用不同合约方向数量、撤单、两次加仓、开空、过期和断开账户验证计划与账本不变量；只有明确确认才可产生模拟fill。

**第三批：模型与界面。** 使用本地mock而非付费API验证019—023的URL路径、历史上下文、配置回填、失败来源、幂等与可见错误；保留真实提供商验证为单独状态。然后点完首页四卡、技能/专家/团队详情、会话、数据库、收藏、比赛/Jev、QUBE/EVO和设置。

**视图矩阵：** 1440×1000、390×844、768×844，并在900/901断点两侧检查导航；检查新建→列表→会话→报告→刷新→前进后退，7预设与背景开关、长输入和发送中按钮。保持Mosha参考，不另换皮肤来掩盖问题。

**继续条件：** P0清零，P1主路径通过独立复现；每一残留P2/P3有明确处置与产品边界。**修改条件：** 任务只定位本地演示时可以减少能力，但必须移除失真的运行中、安装完成、统计可用等暗示。**停止发布条件：** 仍可跨来源写入、单请求杀进程、丢已保存数据或输出未标注的错误统计。

对已复现问题的判断置信度高；并发问题已在两个环境出现，但样本频率不是生产故障概率。真实供应商、真实用户云桌面与跨浏览器业务验收保持UNKNOWN。下一轮只复验已修ID及新变化，不重新堆一份重复审计清单。

**什么现实证据能够证明这个判断是错的？** 在相同固定输入和环境上给出相反的独立可重现结果，或证明产品已发布的范围/前置条件排除了对应输入，并在UI与服务器同时强制了该条件；仅有开发者口头说明或6/6单测不构成反证。
