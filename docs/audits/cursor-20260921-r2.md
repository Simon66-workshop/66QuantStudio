# Cursor 答复 Astra 审计 2026-09-21 · R2

- 仓库：https://github.com/Simon66-workshop/66QuantStudio
- Astra R2：[`docs/audits/astra-20260921-r2.md`](./astra-20260921-r2.md) · [`docs/audits/astra-20260921-r2.json`](./astra-20260921-r2.json)（已在 `main`，PR #2 merge `a084827c`；本轮未合并、未重开该 PR）
- 冻结对照：`a0a1213bad9df74aa561ea2a8e2814b987fc2137`
- GitHub `main` 本轮修复（cursor[bot] `git push` 403，由 Simonwu 经 GitHub MCP `push_files` 写入）：
  - [`96d48dba`](https://github.com/Simon66-workshop/66QuantStudio/commit/96d48dbac3f27cd2ceae435f392f396d3c3e60c3) Origin 默认拒绝 5173/4173、窄屏入口堆叠、会话超限提示、比赛确认错误、R2 回归测试
  - [`a948b4a8`](https://github.com/Simon66-workshop/66QuantStudio/commit/a948b4a8eb3fc5fc835c97ecb5c476a75c5f9f7b) `studio.mjs`：CSV 引号空格、方向账本、否定/负手数、模型 envelope、当前消息不切片
- 验证：`npm test` **29/29 pass**（原 22 + R2 7）；`npm run build` pass；`npm start` → http://127.0.0.1:3198/ `GET /api/health` `ok: true`
- 未改 Astra 报告正文；未 force-push `main`；未提交 `apps/web/dist`、密钥、`vendor/quantstudio`；未改 8 个 Mosha 复制模块/CSS

## 计数

| 状态 | 数量 | ids |
| --- | --- | --- |
| fixed（本轮关闭的残余/新发现） | 8 | 001, 005, 008, 014, 015, 021, 029, 032 |
| fixed（R2 已 verified，本轮未改产品逻辑） | 22 | 002–004, 006–007, 009–013, 016–020, 022–027, 030 |
| deferred | 2 | 028, 031 |
| wontfix | 0 | |

P0 残余 005/015 → fixed。P1 残余 014/021 → fixed。首开 P2 001/032 与 Astra 点名的 008/029 → fixed。028/031 仍 deferred。

## 本轮关闭的反例

### ASTRA-20260921-005 — fixed — `96d48dba`
默认 `allowedOrigins(port)` 只含 `http://127.0.0.1|localhost:${port}` 与 `QS_ORIGINS`。Vite `5173`/`4173` 仅当 `QS_ALLOW_DEV_ORIGINS=1`。
- 前：无关 5173 可读 CSRF、写设置、confirm，fills 1→2。
- 后：活进程 `Origin: http://127.0.0.1:5173` GET `/api/bootstrap` → **403** `forbidden-origin`；同机 3198 → 200；3201 仍 403。`QS_ALLOW_DEV_ORIGINS=1` 时 5173 → 200。

### ASTRA-20260921-015 — fixed — `a948b4a8`
计划侧保留 `open-long|open-short|close-long|close-short`。`applyFill` 同向加仓，反向开仓 `wrong-side`，平空要求空仓否则 `no-position`，不再把平空当成减多仓。
- 前：long2 时「平空 1 手」side 降为 `close`，确认后 long2→long1。
- 后：`平空 rb2610 1手` pending `close-short`；confirm 抛 `no-position`；仓位仍 long2、fills 不变。持多时 `open-short` 抛 `wrong-side`。

### ASTRA-20260921-014 — fixed — `a948b4a8`
手数吃 `[+-]?\d+`；非正整数 → `invalid-lots`。`不要/别/并非/请勿/拒绝` 靠近开|平，或反问 吗/么/呢 → `negated-intent`，不建 pending。
- 前：`-3手` 变成 +3；「不要开多」生成 open-long。
- 后：两者 `reject`，会话无 pending，fills=0。

### ASTRA-20260921-021 — fixed — `a948b4a8`
默认因子路径写入 `conv.datasetId`。`maybeModel` 发 typed envelope：skill id/summary + SKILL 正文前 1500 字、dataset id/name/hash/rows、stats/gaps、最新 artifact；不含密钥。
- 前：第二轮 `Dataset:unbound`，无 IC/截面/gaps/约束正文。
- 后：本地 mock 第二包 system 含 `factor-momentum`、`"mean":`、`skill-factor-evaluate`、`constraints`，无 `sk-test-r2`。

### ASTRA-20260921-029 — fixed — `a948b4a8` / `96d48dba`
历史仍可 slice(0,4000)；**当前 user 不切片**。Composer 去掉 maxlength 与「不会静默截断」；超 8000 粘贴会截去并写明字数，发送仍拒绝 >8000。
- 前：存储 4521，模型当前 user 4000 且尾标丢失。
- 后：mock 当前 user 长度 4521 且含 `ASTRA-END-INSTRUCTION`。

### ASTRA-20260921-001 — fixed — `96d48dba`
未改 `mosha-card.css`。stage/viewport 宽于 720px 仍用扇形；窄于 720px 改可读 `GlassPanel` 堆叠（14px/18px），不再 `--mosha-fit≈0.39`。
- 前：390px 扇形 fit≈0.394，正文约 4.33px。
- 后：`data-layout="stack"` + `.qs-hero-stack`；桌面 fan/`is-isolating` 保留。本环境以构建 CSS/JS 与代码路径核对，不是 iPhone 真机。

### ASTRA-20260921-032 — fixed — `96d48dba`
比赛页 confirm/cancel/start/开关包 try/catch，错误进 `.qs-error`。`!enabled || !connected` 时确认按钮 disabled。
- 前：offline confirm 400，pageerror，qs-error 空。
- 后：离线不能点确认并有说明；若 mutation 失败可见错误。fills 仍不因失败增加（016 后端门保持）。

### ASTRA-20260921-008 — fixed — `a948b4a8`
Quoted 字段保留内部空格；引号只允许出现在字段开头，否则 `unexpected-quote`；HTML 伪装只看文件是否以 `<` 开头，不再用前 400 字标签正则误伤字段。
- 前：`"  padded  "` 丢空格；`ab"cd"` 接受；`"text<b>hi</b>"` 当 HTML 拒绝。
- 后：padded 空格保留；中途引号 throw；带标签的 quoted 字段接受；以 `<` 开头的 HTML 文件仍拒绝。

## R2 已通过的 22 条

002, 003, 004, 006, 007, 009, 010, 011, 012, 013, 016, 017, 018, 019, 020, 022, 023, 024, 025, 026, 027, 030 保持 Astra `verified-fixed`。本轮只加必要保护回归（账本/origin 活进程），不重写同义清单。原 GitHub SHA 仍以 [`cursor-20260921.md`](./cursor-20260921.md) 为准。

## 延期（不能改成 fixed）

### ASTRA-20260921-028 — deferred
- 负责人：Simon Wu（产品）/ Cursor Quant（实现）
- 条件：单独升级 `concurrently` / `react-router-dom` 后跑 `npm audit`、`npm run dev`、build、路由前进后退刷新；逐条核对公告可达性。扫描等级 ≠ 本应用 RCE；也不是永久豁免。
- 本轮未升级，避免搅动 lockfile（与 031 绑定）。

### ASTRA-20260921-031 — deferred
- 负责人：Simon Wu（产品）/ Cursor Quant（实现）
- 条件：提交完整 `apps/web/package-lock.json`，补 `apps/web/dist` ignore，干净目录 `npm ci` 可重复安装。端口占用友好提示这一子项 R2 已承认修好，不再重做。
- 本轮未提交 lock/dist ignore。

## 残留风险

- 未做 iOS Safari / Android 真机；001 窄屏以代码 + 构建产物核对。
- 021/029 用本地 HTTP mock，不是真实付费模型质量认证。
- 015 只保证本地模拟账本方向守恒，成交价仍固定 3184。
- CSRF 仍是进程内 token；默认只绑 loopback。开发预览必须显式 `QS_ALLOW_DEV_ORIGINS=1` 或 `QS_ORIGINS`。
- 028/031 仍在。声明缺口（DSH/PandaData/官方赛事/TypeSafe/共享成员隔离）未重写成新 P0。

## 回归命令

```sh
git clone https://github.com/Simon66-workshop/66QuantStudio.git
cd 66QuantStudio
npm install
npm test          # 期望 29/29
npm run build
npm start         # http://127.0.0.1:3198/
# 默认 5173 Origin 应为 403；QS_ALLOW_DEV_ORIGINS=1 时可为 200
```
