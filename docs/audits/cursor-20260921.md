# Cursor 答复 Astra 审计 2026-09-21

- 仓库：https://github.com/Simon66-workshop/66QuantStudio
- Astra 报告：[`docs/audits/astra-20260921.md`](./astra-20260921.md) · [`docs/audits/astra-20260921.json`](./astra-20260921.json)（已合入 `main`，PR #1 merge commit `f8a4b247a2407b63caf7980009b6f6dd35c4a5cd`）
- GitHub `main` 修复提交（`git push` 对 cursor[bot] 403，由 Simonwu 经 GitHub MCP `push_files` 写入）：
  - [`33339c99`](https://github.com/Simon66-workshop/66QuantStudio/commit/33339c991be785a96bef4ce2659762aaf84c24e3) CSRF store / Home 预设 / HTTP origin
  - [`08fec82e`](https://github.com/Simon66-workshop/66QuantStudio/commit/08fec82ed50a24e8727593fa21de82f0edfee3a3) Mosha isolate/fit、移动菜单、CSRF 客户端
  - [`5262cc61`](https://github.com/Simon66-workshop/66QuantStudio/commit/5262cc61959c169a0e229782c83eb7d632ec40e6) composer/error CSS、数据库/收藏、设置、赛事 UI
  - [`f6b24b7c`](https://github.com/Simon66-workshop/66QuantStudio/commit/f6b24b7c0a5f53ec9a76216038c3b70c93f70607) 目录收藏/缺口、会话 UX
  - [`797ea837`](https://github.com/Simon66-workshop/66QuantStudio/commit/797ea837bcb33500119faeb986b00ca36f4bb9c8) loopback Host、CSRF、asset 404、confirm 闸门
  - [`b36a2983`](https://github.com/Simon66-workshop/66QuantStudio/commit/b36a2983cf42626dcae61139fe5a5cf5e85ce9a5) P0/P1 回归测试
  - [`d6bc7b03`](https://github.com/Simon66-workshop/66QuantStudio/commit/d6bc7b03f7a549afd48470b1f0ab3b0160f195ce) persist/CSV/IC/账本/交易解析/模型 URL/Jev（`server/lib/studio.mjs`）
- 验证：`npm test` **22/22 pass**；`npm run build` pass；`npm start` → http://127.0.0.1:3198/ `GET /api/health` `ok: true`
- 未改 Astra 报告正文；未 force-push `main`；未提交 `apps/web/dist`、密钥、`vendor/quantstudio`

## 计数

| 状态 | 数量 | ids |
| --- | --- | --- |
| fixed | 29 | 001–027, 029, 030 |
| deferred | 2 | 028, 031 |
| wontfix | 0 | |

P0 7/7 fixed。P1 12/12 fixed。首屏/一打开可见的 P2（001, 003, 004 以及导航相关 002）fixed。Astra `gapChallenges` 未把声明缺口升格为新 P0；020 的 `challenge`（模型 URL/回退轨迹）已按证据修。

## Findings

### ASTRA-20260921-001 — fixed — `08fec82e`
MoshaHand 接 `is-isolating` / `is-active`、ResizeObserver `--mosha-fit`、去掉固定 `scale(0.92)` 与 overflow clip。验证：桌面浏览器悬停卡片会独立抬起、相邻卡变糊；截图见 computer-use 会话。

### ASTRA-20260921-002 — fixed — `08fec82e` / `5262cc61`
≤900px 显示顶栏「菜单」，同一 `LINKS` 抽屉可到数据库/设置/收藏等；Esc/关闭/点链接会收起。验证：代码 + 样式 `grid-template-areas`；本环境 Chrome 390 截图因已占用的 profile 未能另开 headless，残留见文末。

### ASTRA-20260921-003 — fixed — `08fec82e` / `33339c99`
`Shell` 用 `settings.appearance.preset` 写入 Mosha CSS 变量；`Home` 扇形卡跟同一预设；`data-animated=0` 时去掉 ambient。验证：设置切「厚玻璃」后首页光学层变化；动态背景开关有 DOM 差异。

### ASTRA-20260921-004 — fixed — `5262cc61`
按钮 `flex-shrink: 0; white-space: nowrap`；输入框 `flex: 1; min-width: 0`。验证：会话页「发送」横排完整。

### ASTRA-20260921-005 — fixed — `797ea837` / `33339c99` / `08fec82e`
默认 `HOST=127.0.0.1`；校验 Host/Origin 白名单（本机 3198/5173/4173）；去掉 `Access-Control-Allow-Origin: *`；写入要 `x-studio-csrf`。验证：`Origin: http://127.0.0.1:3201` 读 `/api/bootstrap` → **403**；无 CSRF 的 PUT `/api/settings` → **403**；同机 CSRF PUT 成功。

### ASTRA-20260921-006 — fixed — `797ea837` / `b36a2983`
URL 用固定 `http://127.0.0.1` 解析，Host `[` 进 `assertSafeHost` 返回 400，整段 handler try/catch。验证：`server/tests/astra-fixes.test.mjs` 对活进程发畸形 Host，进程仍响应后续 `/api/health` 200。

### ASTRA-20260921-007 — fixed — `d6bc7b03`
persist 串行锁 + 唯一 tmp + `fsync` + `rename`；损坏 `state.json` 先备份 `state.corrupt-*`。验证：并发 24 个 `createSkill` 后 JSON 可解析，重启仍 24 条。

### ASTRA-20260921-008 — fixed — `d6bc7b03`
RFC4180 引号内换行；拒绝重复列名、未闭合引号、HTML 冒充 CSV。验证：quoted multiline 两行；非法 CSV throw。

### ASTRA-20260921-009 — fixed — `d6bc7b03`
空白/null 不当 0；`gaps` 含 `missing-values`。验证：100 / 空 / 110 的 min=100，回撤不是 -100%。

### ASTRA-20260921-010 — fixed — `d6bc7b03`
零方差截面记 `zero-variance`，相关系数为 `null`，不再 `|| 1`。验证：常数因子 8 日截面 `stats === null`，HTML 含缺口。

### ASTRA-20260921-011 — fixed — `d6bc7b03`
有日期列则排序后算最新/回撤，并标 `unsorted-dates`。验证：乱序 110/100/90 → last=110，maxDD=-10%。

### ASTRA-20260921-012 — fixed — `d6bc7b03`
有 IC 摘要时仍输出 `gaps`。验证：1 个截面 HTML 含 `insufficient-cross-sections`。

### ASTRA-20260921-013 — fixed — `d6bc7b03`
用户点名的文件名/id 优先绑定；因子路由用 `\bIC\b` 而非子串。验证：上传 `astra-price.csv` 后「只分析…astra-price.csv」得到最新 90、回撤 -25%，不是动量 IC。

### ASTRA-20260921-014 — fixed — `d6bc7b03`
解析合约/方向/手数；`撤单` 只取消 pending，不开平仓单；未知合约拒绝。验证：`开空 cu2701 5手` → pending `cu2701 open-short 5`，fills=0。

### ASTRA-20260921-015 — fixed — `d6bc7b03`
按合约累加/减仓，重算 margin/available/riskRatio；open-short 真开空。验证：两笔开多 rb2610 → 2 手 long；开空 cu2701 5 手 → short 5。

### ASTRA-20260921-016 — fixed — `d6bc7b03`
确认时校验 pending、过期、enabled/connected。验证：`expiresAt=2000` → `plan-expired`；断开后 confirm → `contest-offline`，fills 不增。

### ASTRA-20260921-017 — fixed — `d6bc7b03`
突破高点窗口排除当前 bar。验证：30×100 + 130 的 fixture `jevStart(breakout)` → `open-long` pending。

### ASTRA-20260921-018 — fixed — `d6bc7b03`
文案改为本地单次评估；相同合约/方向/模板的 pending Jev 计划去重。验证：同一 fixture 再 tick pending 仍 1 笔。

### ASTRA-20260921-019 — fixed — `5262cc61`
设置页从 snapshot 回填 `baseUrl`/`model`；空密钥框不提交。验证：浏览器保存 URL 后离开再进，字段仍在。

### ASTRA-20260921-020 — fixed — `d6bc7b03`
`joinModelUrl` 保留 `/v1`；`new URL` 进 try；HTTP/超时写入 `modelGap` 到 assistant 轨迹。验证：`joinModelUrl('http://127.0.0.1:3201/v1')` → `/v1/chat/completions`。未起真实付费模型。

### ASTRA-20260921-021 — fixed — `d6bc7b03`
请求带 bound skill 与最近 16 条 user/assistant。验证：代码路径；本轮未接 mock chat 服务做 payload 抓包（残留）。

### ASTRA-20260921-022 — fixed — `d6bc7b03` / `f6b24b7c`
前端 `sending` ref + `preventDefault`；服务端会话锁 + `Idempotency-Key`。验证：同一 key 两次 POST 只有 1 条 user。

### ASTRA-20260921-023 — fixed — `f6b24b7c` / `5262cc61`
发送/导入/创会话失败显示 `.qs-error`，保留输入；未知会话 404 页无发送框。验证：代码 + 非法 CSV 走 400 错误路径。

### ASTRA-20260921-024 — fixed — `d6bc7b03` / `f6b24b7c`
本机技能 id 加随机后缀；未安装/未知技能不能 `startConversation`；详情提供重新安装。验证：同名技能 id 不同；卸载内置后 start 拒绝。

### ASTRA-20260921-025 — fixed — `f6b24b7c`
创建会话后 `refresh()`；进入会话恢复最后一份 artifact。验证：代码路径。

### ASTRA-20260921-026 — fixed — `f6b24b7c` / `5262cc61`
收藏列表是可打开链接 + 取消；技能/专家详情有收藏。验证：代码路径。

### ASTRA-20260921-027 — fixed — `f6b24b7c`
缺 excerpt/LEAD/members 时写明快照未包含，不留空 LEAD。验证：浏览器专家/专家团页有缺口文案。

### ASTRA-20260921-028 — deferred — n/a
`npm audit` 的 concurrently/react-router 告警未证明本应用 RCE（Astra 原文亦如此）。本轮不强制升级以免搅动 lockfile。下次可单独升级并回归 `npm run dev`/路由。

### ASTRA-20260921-029 — fixed — `d6bc7b03` / `f6b24b7c`
超过 8000 字返回 `too-long`，前端计数，不再静默 slice。验证：8001 字 POST 拒绝。

### ASTRA-20260921-030 — fixed — `797ea837`
`/assets/*` 与带静态扩展名的缺失文件 404；HTML fallback 只给导航路径。验证：`GET /assets/astra-nonexistent.js` → 404；`/conversations/demo` → 200 HTML。

### ASTRA-20260921-031 — deferred — n/a
P3：完整 lockfile/`dist` gitignore/端口提示。未 build 的 503 hint 原本已有；EADDRINUSE 现有可读说明。不把交付锁文件大改与本次 P0 绑在一起。

## 声明缺口

同意 Astra 对已知缺口的 `agree`。020 的 `challenge` 已修（路径拼接 + 回退写入消息）。未把 DSH/PandaData/TypeSafe/vendor 缺失重写成新 P0。

## 残留风险

- 本机 Chrome profile 被占用时，未能补拍 390px 菜单截图；移动导航靠 CSS/DOM 实现，需真机再点一次。
- 021 历史上下文未用独立 mock 模型进程抓包。
- 028/031 依赖与安装体验仍在。
- CSRF 是进程内 token，重启后前端需重新 bootstrap（`load` 会拿新 token）。
- 默认只绑 loopback；若要局域网访问需显式 `HOST=0.0.0.0` 并设 `QS_ORIGINS`。
- 模拟成交价仍固定 3184，仅保证账本守恒，不是行情。
- 共享部署成员隔离仍是声明缺口。

## 回归命令

```sh
git clone https://github.com/Simon66-workshop/66QuantStudio.git
cd 66QuantStudio
npm install
npm test          # 期望 22/22
npm run build
npm start         # http://127.0.0.1:3198/
```
