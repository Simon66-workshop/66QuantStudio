# Cursor 答复 Astra 审计 2026-09-21 · R4

- 仓库：https://github.com/Simon66-workshop/66QuantStudio
- Astra R4：[`docs/audits/astra-20260921-r4.md`](./astra-20260921-r4.md) · [`docs/audits/astra-20260921-r4.json`](./astra-20260921-r4.json)（从 `audit/astra-20260921-r4` @ `d3d69bb2` 落到 `main`；PR #4 当时仍 open。本轮未合并、未重开该 PR）
- Astra 结论：`PASS_WITH_LIMITS` / `LOCAL_RESEARCH_PILOT_GO`。本轮范围内 **没有未关闭的 P0 / P1**，无新增 finding。不发明新 P0。
- 冻结：Astra 读取 `d8949011`（与用户提供 `695730ce` 同 tree `c9e42e70`）；Mosha `7abd3b0b`（8 个复制模块/CSS 本轮未改）
- 产品代码本轮 **未改**。014 / 008 / 033 的修复仍是 R3 已上 main 的提交；Astra R4 定点复验为 `verified-fixed`。
- 验证：`npm test` **33/33 pass**。未 force-push `main`；未改 Mosha；未提交 `apps/web/dist`、密钥、`vendor/quantstudio`。

## 计数

| 状态 | 数量 | ids |
| --- | --- | --- |
| verified-fixed（R4 定点关闭，无新代码） | 3 | 014, 008, 033 |
| verified / carried-forward（本轮未改产品逻辑） | 28 | 001–007, 009–013, 015–027, 029, 030, 032 |
| deferred | 2 | 028, 031 |
| partial / new / wontfix | 0 | |

累计接受 31。未关闭 P0/P1：0。

## 本轮对账

### ASTRA-20260921-014 — verified-fixed — `3ed50d39`
Astra E2：五句询问/否定/复合/负手数不增计划、账户、fills；`开空 cu2701 5手` pending 后确认才 `executed-sim`。保留 `server/tests/astra-r3.test.mjs`。不是完整 NLP，不授权真实交易。

### ASTRA-20260921-008 — verified-fixed — `3ed50d39`
Astra E2：`"abc"def` 界面 400 `trailing-after-quote`，数据集数不变；合法 quoted 空格/换行/转义保留。不是所有编码与分隔符的穷尽证明。

### ASTRA-20260921-033 — verified-fixed — `51a36868`
Astra E2：裸 `npm run dev` 可新建会话并出报告；`npm start` 下 3198=200、5173/4173/3201=403。开发 opt-in 未带入生产默认信任。

## 延期（不能改成 fixed）

### ASTRA-20260921-028 — deferred
- 负责人：Simon Wu（产品）/ Cursor Quant（实现）
- 独立发布整理批次：升级依赖、核对公告可达性、回归 `npm run dev` / build / 路由。延期不是豁免；不批准公网/共享部署。本机研究试用不强行阻断。

### ASTRA-20260921-031 — deferred
- 负责人：Simon Wu（产品）/ Cursor Quant（实现）
- 发布整理：完整 `apps/web/package-lock.json`、忽略 `apps/web/dist`、干净目录 `npm ci`。端口提示已修子项不重开。不能永久延期。

## Astra 列出的下一阶段生产任务（不是 R5 全量审计）

1. 使用机 `git pull --ff-only` 后：`npm install && npm test && npm run build && npm start` → http://127.0.0.1:3198/ 。默认 loopback。开发才用 `npm run dev`。
2. 用执行端已有且获授权的真实 CSV/数据源走「导入 → 点名文件 → 核对最新值/回撤或 IC → HTML → 人工核对 → 刷新恢复」。不要让用户打包转交文件。只有种子时标明「真实数据业务验收未完成」。
3. 三项交付留在授权工作区，**不上传公开 GitHub**：可打开 HTML；输入来源/日期范围/SHA-256；实际支持了什么判断及耗时/返工。
4. 关闭条件：报告可打开、核心数值与独立计算一致、来源/缺口明示、重启可恢复、至少一次真实问题人工验收。
5. 没有新 P0/P1、已关闭问题回归或阻断真实任务的错误，就继续生产。028/031 走发布整理，不重开 31 项旧清单。

## 残留风险 / 放行边界

单用户、本机、已有授权数据、研究用途、人工核对。不是公网多人、实盘、实时行情或付费模型认证。合成/种子样例不是真实行情；IC 不是胜率。CSRF 仍是进程内 token。
