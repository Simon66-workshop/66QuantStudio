# Astra 审计 2026-09-21 · R4 收口与生产交接

**结论：PASS_WITH_LIMITS / LOCAL_RESEARCH_PILOT_GO。014、008、033 三项定点复验通过；31 项累计接受，028 / 031 保持延期。在本轮及承接的审计范围内，没有未关闭的 P0 / P1。不再扩展全量审计，下一阶段交付真实研究任务。**

放行范围：**单用户、本机、已有授权数据、研究用途、人工核对**。不是公网多人部署、真实交易、实时行情或实际付费模型认证；不是“项目已经实现经营价值”。

## 1. 冻结与范围

- 仓库：`Simon66-workshop/66QuantStudio`。
- 用户提供：`695730ce8f0272be87c5f4e63b2658a245b005cf`。
- 实际读取 main：`d8949011a6e754e122ebe2d610d6588520de73d0`。二者 tree 同为 `c9e42e706b888903f8bbd831d3955516801390f6`，差异文件 0；不是另换产品版本。
- 入口：[cursor-20260921-r3.md](./cursor-20260921-r3.md)；前轮：[astra-20260921-r3.md](./astra-20260921-r3.md)。历史报告不覆盖。
- 分支：`audit/astra-20260921-r4`。最终相对 main 仅新增本轮 MD / JSON；不改产品代码、锁文件或旧报告。临时验证 workflow 从最终文件差异移除，不合并 main。
- 本轮只关闭 008 / 014 / 033，补相邻安全门与“导入 → 报告 → 刷新恢复”。28 项旧验收明确承接，不冒充本轮重新全测。

## 2. 实际验证

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| 冻结版本 clone / npm install | 成功，退出码 0 | E1：base-sha.txt、npm-install.log / .exit |
| npm test | 33 / 33，0 失败；本地隔离副本另跑也通过 | E1：npm-test.log / .exit |
| npm run build | 退出码 0；本地隔离副本另跑也通过 | E1：npm-build.log / .exit |
| npm start / 3198 | 启动，完成真实浏览器操作 | E2：npm-start.log、r4-results.json |
| npm run dev / 5173 | 未人工补环境变量，创建会话、发送、报告渲染成功 | E2：npm-dev.log、r4-dev-working-report.png |
| 定点浏览器检查 | 8 / 8；pageErrors = 0 | E2：browser.log、browser.exit、r4-results.json |
| 非法文件错误态 | 预期 400，可见 trailing-after-quote，数据集数不变 | E2：r4-csv-rejected.png |

Ubuntu Linux x86_64；Node 22.16.0；npm 10.9.2；Chrome 152.0.7977.82；桌面 1440×1000、窄屏 390×844。Browser plugin 未提供，使用 Playwright 操作真实 Chrome。

**审计工具失败不伪装成产品问题或全绿：**E1 首次浏览器 7 / 8，唯一失败是脚本错误要求标题等于 `66QuantStudio`，实际正确标题为 `66QuantStudio · Quant · Work · Trade`。按仓库 HTML 修正断言，同一份产品重跑得到 E2 的 8 / 8。本地 Chromium 另一次尝试被环境策略 `ERR_BLOCKED_BY_ADMINISTRATOR` 拦截，未绕过、未算产品失败；最终浏览器结论来自 GitHub 隔离环境。唯一控制台 error 是主动导入非法 CSV 的预期 HTTP 400，不声称“控制台绝对零错误”。

## 3. 三项关闭

### ASTRA-20260921-014 — verified-fixed

- 闸门 / 原级别：product / P1。
- 位置：`server/lib/studio.mjs` 的 `parseTradeIntent`、`localReply`；会话和比赛页。
- 复现：发送 `开多 rb2610 100手可以吗？`、`开多 rb2610 3手的风险是什么？不要下单。`、`开多 rb2610 1手，开空 cu2701 2手`，再回归 `开多 rb2610 -3手`、`不要开多 rb2610 3手`。
- 期望 / 实际：五句均不增加计划、不改变账户、不增加 fills，有中文拒绝说明。
- 正向对照：`开空 cu2701 5手` 得到 `open-short / cu2701 / 5 / pending`，fills=0；实际点击确认后 `executed-sim`、空仓5手、fills=1。另建 rb2610 计划点击取消，fills 仍为1。测试结束关闭比赛模式。
- 关闭边界：原反例与明确单条命令合同；不是完整 NLP 下单系统，不授权真实交易。
- 保留测试：`server/tests/astra-r3.test.mjs` 与 E2 的 `014-intent-and-confirmed-simulation`。

### ASTRA-20260921-008 — verified-fixed

- 闸门 / 当前残余级别：data / P2。
- 位置：`server/lib/studio.mjs` 的 `parseCsv`、`addDataset`；数据库导入。
- 复现：界面导入 `a,b\n"abc"def,1\n`；再导入 quoted 内部空格、HTML 字符文本、字段内换行和双引号转义的合法三行 CSV。
- 期望 / 实际：非法文件 400 `trailing-after-quote`，界面显示错误且数据集数不变；合法三行导入成功，`  padded  ` 两侧空格、字段内换行、转义引号正确保留。
- 关闭边界：已登记的 CSV 反例闭环，不等于所有编码、分隔符和大文件已穷尽测试。
- 保留测试：现有 R3 CSV 回归与 E2 的 `008-real-CSV-import`。

### ASTRA-20260921-033 — verified-fixed

- 闸门 / 原级别：product / P2；涉及后端启动配置。
- 位置：`package.json` 的 `dev` / `dev:api` / `start`；`server/lib/http-guard.mjs`。
- 复现：清除外部 QS_ORIGINS / QS_ALLOW_DEV_ORIGINS，直接 `npm run dev`，打开 5173，新建会话、发送种子分析需求、查看报告。
- 期望 / 实际：新建、消息、HTML 渲染成功；此前 3198 的研究会话在服务重启后仍存在。
- 反向对照：停 dev 再 `npm start`，3198 来源 bootstrap=200；5173 / 4173 / 3201 均403。开发修复没有恢复生产默认信任。
- 保留测试：现有 R3 launcher 回归与 E2 的 `033-exact-dev-launcher-production-output`、两次005来源检查。

## 4. 已生成可打开的研究产物

### CSV 导入 → 报告 → 刷新恢复

真实界面导入 `astra-r4-synthetic-research.csv`，创建会话、点名文件生成右侧 HTML，刷新后报告恢复。

```csv
date,close
2026-09-14,100
2026-09-15,110
2026-09-16,120
2026-09-17,90
```

**这是合成验收数据，不是真实行情。**报告输出4行、最新90、均值105、最大回撤-25.00%，与独立计算相符，有研究免责声明。E2 保留完整 `synthetic-research-report.html`、输入 CSV 和 `r4-research-delivered.png`，不是只有截图。

- 输入 SHA-256：`d13da780ab134c7b09d0b681a5b4bdaa2515faddd99b12ccb05ac14d97cd8af1`。
- HTML SHA-256：`0655e8c3403b3beb5712f1b44687ae0c10ffc057537e5ffe7ebafd6a9c5662db`。
- 隔离会话 `c-0952e071`，产物 `a-8c1bb08e`；不是在辉少实际工作区创建的会话。

### 原有技能报告

从 `skill-factor-evaluate` 详情开始会话，实际生成 `seed-factor-research-report.html`。仓库种子640行、80个有效截面；报告显示平均 Pearson IC 0.9353、标准差0.0276、IR 33.904。独立 Python 重算为0.935314813971839、0.027587113467268488、33.904047811365615，显示值一致。

**这只是种子统计量，不能当作真实策略表现，更不能写成胜率。**未接 PandaData 或实际付费模型。

- HTML SHA-256：`c403079f1348cd81739880e45d0f712a2921bfde72ce8da87fb31258ba98e680`。
- 隔离会话 `c-e558c4e7`，产物 `a-0773e6ba`。
- dev 另生成 csi300 种子报告：420行、末行日期2025-08-11、值4226.65，回撤显示-4.60%；独立计算-0.04595874922021215。这是种子末值，不是当前沪深300行情。

## 5. 五条闸门与延期

| 闸门 | 本轮判断 | 边界 |
| --- | --- | --- |
| 产品 | 本地研究主路径可试用 | 导入→会话→HTML→恢复实跑；不承诺所有技能均具完整业务能力 |
| 设计 | 首屏与窄屏回归通过 | 390px四卡可读、无横向溢出；桌面保留扇形。Mosha八个复制模块与R3逐字节一致，承接参考SHA `7abd3b0b811f483f8d172f15e843836496824b3f` 的前轮比对 |
| 后端 | 修复相关路径通过 | 33/33、build/start/dev；不是长期负载或容灾认证 |
| 安全 | 本机研究可受限试用 | 生产拒绝无关开发来源，确认门保持；028未消除，不批准公网部署 |
| 数据 | 固定样例算数与报告一致 | 合成/种子不是实时行情，不替代真实数据来源核验 |

**028 — deferred，未关闭。**依赖升级与公告可达性仍由 Simon Wu / Cursor Quant 跟踪。本轮没有重扫并宣称依赖安全。延期不是豁免；本机研究试用不强行阻断，扩展共享/公网前必须处理。

**031 — deferred，未关闭。**安装后仍见 `apps/web/package-lock.json` 修改、`apps/web/dist/` 未忽略。固定构建能跑不代表可重复发布。完整锁文件、dist ignore、干净目录 npm ci 纳入发布整理批次，不能永久延期；端口提示已修子项不重开。

DSH、vendor、紧凑catalog、PandaData/官方赛事、本地Jev、共享成员隔离等声明承接前轮，不重写新P0。真实模型、TypeSafe、实盘、iOS/Android真机未在本轮重新验证。

## 6. 给 Cursor：生产交付，不是 R5 全量审计

**第一步：**实际使用机器 `git pull --ff-only` 后按已验证命令启动，保留默认loopback。先用现有工作台完成一份研究任务，不重构产品、不增加Agent或新建平台。

```sh
npm install && npm test && npm run build && npm start
# 本机试用：http://127.0.0.1:3198/
# 开发才用 npm run dev，不把开发来源授权带入常驻生产环境。
```

**首个生产任务：**从执行端已有且获授权的真实CSV或既有授权数据源选择一个明确问题，走“导入→点名文件→核对最新值/回撤或相关IC→报告→人工核对→重新打开”。不要让用户打包转交文件。只有种子时照常交付可运行演示，明确“真实数据业务验收未完成”，不把示例改名冒充生产成果。

**只收三项交付：**可打开的HTML；输入来源/日期范围/SHA-256；一句实际支持了什么判断及耗时/返工记录。敏感输入和完整报告留在授权工作区，不上传公开GitHub。

**关闭条件：**使用者能打开报告，核心数值与独立计算一致，来源/缺口明示，重启仍可恢复，完成至少一次真实问题的人工验收。满足后称首轮生产验收完成；本次完成的是工程收口与受限试用放行。

**停止循环审计：**没有新P0/P1、已关闭问题回归或阻断真实任务的错误，就继续生产。028/031在独立发布整理批次处理，不重开31项旧清单。某项阻断生产时只修该项并做受影响路径回归。

## 7. 证据索引

- **E1**：[冻结安装及首次运行](https://github.com/Simon66-workshop/66QuantStudio/actions/runs/35628912163)，artifact `astra-20260921-r4-closure`，ID `10654180993`。run整体失败仅因错误标题断言，install/test/build分别成功。
- **E2**：[同版本修正审计断言后验证](https://github.com/Simon66-workshop/66QuantStudio/actions/runs/35629358060)，artifact `astra-20260921-r4-proof`，ID `10654236531`，运行成功。含脚本、8项结果、7张截图、2份HTML、CSV、日志、SHA256SUMS。
- E2 ZIP SHA-256：`a82165c8043f36544a03d3f26c6a07b205abe2b12a7ccf0e09d8e1b84bacf029`。
- E2 `r4-results.json` SHA-256：`48b6641cd8283eab6bfc62c1e4ac387108e4f4a825c11ad7e6b228c18c8057a7`；脚本SHA-256：`4763f4e528b5e116aeef406bc0f436609bfa13ec9cd32304096978f61948b581`。
- 下载后逐项核对E2哈希。固定源码95文件一致，仅排除npm install生成变化的前端锁文件，不排除产品源文件。临时服务已结束，不声称替用户常驻部署。
- artifact保留到2026-12-20；本MD/JSON保留复现、结论、数值、哈希。报告和证据无实际API密钥。
